---
phase: 01-foundation-schema-auth-rls
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/lib/schemas/displayName.ts
  - src/lib/schemas/join.ts
  - src/lib/auth/session.ts
  - src/lib/auth/admin.ts
  - src/app/actions/join.ts
  - src/app/actions/locale.ts
  - src/app/[locale]/page.tsx
  - src/app/[locale]/join/page.tsx
  - src/app/[locale]/matches/page.tsx
  - src/app/[locale]/bracket/page.tsx
  - src/app/[locale]/leaderboard/page.tsx
  - src/app/[locale]/me/page.tsx
  - src/app/[locale]/not-found.tsx
  - src/app/[locale]/layout.tsx
  - src/app/admin/layout.tsx
  - src/app/admin/page.tsx
  - src/app/admin/403/page.tsx
  - src/components/auth/JoinForm.client.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/Wordmark.tsx
  - src/components/layout/LocaleTogglePill.client.tsx
  - src/components/layout/BottomTabBar.tsx
  - src/components/layout/EmptyStateCard.tsx
  - src/components/ui/FormField.tsx
  - messages/he.json
  - messages/en.json
autonomous: false
requirements:
  - FND-02
  - FND-06
  - I18N-01
  - I18N-03
  - I18N-04
  - I18N-05
  - I18N-06
  - I18N-07
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07

must_haves:
  truths:
    - "User can visit /he/join, enter a display name + the correct invite code, and be redirected to /he/"
    - "After redirect, the user's profile row exists in `profiles` table keyed on auth.uid()"
    - "Session persists across browser refresh and across days (anonymous session via @supabase/ssr middleware)"
    - "Wrong invite code shows inline error '⚠ הקוד שגוי...' / '⚠ Wrong code...' (D-03)"
    - "Display name conflict (race or duplicate) shows inline error '⚠ השם הזה כבר תפוס...' (D-09)"
    - "Joining with display_name == ADMIN_DISPLAY_NAME creates a profile with is_admin=true"
    - "Visiting any /[locale]/* route without a profile redirects to /[locale]/join"
    - "Visiting /admin/* without is_admin=true redirects to / (which then redirects to default locale)"
    - "Locale toggle pill switches /he/foo <-> /en/foo and persists the choice via cookie + profiles.locale"
    - "Header shows the active locale's wordmark; bottom tab bar shows 4 tabs (Matches/Bracket/Leaderboard/Me) flipped via <html dir>"
    - "Bracket and Leaderboard tabs render an EmptyStateCard with the placeholder copy from UI-SPEC, not 404s"
    - "All UI strings live in messages/he.json and messages/en.json — no hardcoded literals in components"
    - "Phase 1's I18N-06 coverage is structural-only: hero sub-wordmark uses `<p lang>` for bidi correctness; mixed-direction content stress-test (deep nesting, embedded LTR codes in RTL paragraphs) is deferred to Phase 2 QA-03"
  artifacts:
    - path: "src/app/actions/join.ts"
      provides: "joinPool Server Action — invite-code + signInAnonymously + profile insert"
      contains: "signInAnonymously"
    - path: "src/lib/auth/session.ts"
      provides: "getCurrentMember, requireMember, requireAdmin helpers"
      contains: "getClaims"
    - path: "src/app/admin/layout.tsx"
      provides: "Server-side admin gate (D-06)"
      contains: "requireAdmin"
    - path: "src/components/layout/BottomTabBar.tsx"
      provides: "4-tab bottom navigation with auto-flipping order"
      contains: "Matches"
    - path: "src/components/layout/LocaleTogglePill.client.tsx"
      provides: "Locale switcher with cookie + profile persistence"
      contains: "NEXT_LOCALE"
    - path: "messages/he.json"
      provides: "Full Hebrew copy bundle for Phase 1"
      contains: "הצטרפו"
    - path: "messages/en.json"
      provides: "Full English copy bundle for Phase 1"
      contains: "Join the Pool"
  key_links:
    - from: "src/app/actions/join.ts"
      to: "src/lib/supabase/server.ts"
      via: "createClient() + supabase.auth.signInAnonymously()"
      pattern: "signInAnonymously"
    - from: "src/app/actions/join.ts"
      to: "process.env.INVITE_CODE"
      via: "Exact string equality check"
      pattern: "process.env.INVITE_CODE"
    - from: "src/app/actions/join.ts"
      to: "process.env.ADMIN_DISPLAY_NAME"
      via: "Normalized equality determines is_admin"
      pattern: "ADMIN_DISPLAY_NAME"
    - from: "src/app/admin/layout.tsx"
      to: "src/lib/auth/session.ts"
      via: "requireAdmin() server gate"
      pattern: "requireAdmin"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/layout/Header.tsx + BottomTabBar.tsx"
      via: "Rendered in layout chrome"
      pattern: "BottomTabBar"
notes:
  - "W7 accepted risk: if `signInAnonymously()` succeeds but the profiles INSERT fails for a non-unique-violation reason, the action calls `supabase.auth.signOut()`. If that signOut fails too, an orphan auth.users row persists (anonymous; no profile). Phase 2 admin tooling (ADM-05 user-merge / cleanup) handles reconciliation. Not blocking for Phase 1 ship — orphans are harmless to family-scale traffic and the join flow remains resumable."


---

<objective>
Wire the full Phase 1 user-facing surface: the invite-code → `signInAnonymously()` → `profiles` insert join flow, session helpers, the admin server-side gate, the localized chrome (header + wordmark + locale-toggle pill + bottom tab bar), and the four placeholder/active tab pages (`/matches`, `/bracket`, `/leaderboard`, `/me`). All copy lives in `messages/{he,en}.json` per I18N-04 and exactly matches UI-SPEC §"Copywriting Contract."

Purpose: This is the user-facing payoff of Phase 1. A family member visits `/he/`, gets the join form, picks a name + enters the invite code, lands in the bilingual shell with working tab nav and locale toggle. Their session persists. ROADMAP success criteria #1 and #2 are satisfied by this plan.

Output:
- A working `/[locale]/join` page with `useActionState`-driven inline errors
- `joinPool` Server Action that gates on `INVITE_CODE`, calls `signInAnonymously()`, inserts into `profiles`, sets `is_admin` when display name matches `ADMIN_DISPLAY_NAME`
- Session helpers (`getCurrentMember`, `requireMember`, `requireAdmin`) using `getClaims()` (never `getSession()`)
- Header (wordmark + locale toggle pill) + bottom tab bar (4 tabs, auto-flipping order)
- Placeholder pages with localized empty-state copy from UI-SPEC
- Admin route shell at unlocalized `/admin/` with server-side gate (D-05, D-06)
- Full `messages/{he,en}.json` bundles for Phase 1
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-schema-auth-rls/01-CONTEXT.md
@.planning/phases/01-foundation-schema-auth-rls/01-RESEARCH.md
@.planning/phases/01-foundation-schema-auth-rls/01-UI-SPEC.md
@.planning/phases/01-foundation-schema-auth-rls/01-01-PLAN.md
@.planning/phases/01-foundation-schema-auth-rls/01-02-PLAN.md
@.planning/phases/01-foundation-schema-auth-rls/01-03-PLAN.md
@CLAUDE.md

<interfaces>
<!-- From Plan 01 (already implemented and available) -->

src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient<Database>>;
// Use in RSC + Server Actions. Reads/writes cookies via next/headers.
```

src/lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient<Database>;
// Use in client components only.
```

src/lib/i18n/routing.ts:
```typescript
export const routing = defineRouting({ locales: ['he','en'], defaultLocale: 'he', ... });
export const { Link, redirect, usePathname, useRouter, getPathname } = createNavigation(routing);
```

<!-- From Plan 02 (already implemented and available) -->

src/types/supabase.ts:
```typescript
export type Database = {
  public: {
    Tables: {
      profiles: { Row: { user_id, display_name, display_name_normalized, locale, is_admin, joined_at }, Insert: ..., Update: ... };
      tournament: ...;
      teams: ...;
      // ... all 9 tables
    };
  };
};
```

<!-- Patterns to follow (from RESEARCH §"Code Examples") -->

joinPool action signature: `async function joinPool(prevState, formData) -> { error?, issues? } | redirects`
Session helper pattern: `getClaims()` not `getSession()` — see CLAUDE.md PITFALLS

<!-- From UI-SPEC §"Per-Surface Component Contracts" -->

Header: 56px tall, fixed, bg --zc-card, inline-end is locale pill, inline-start is wordmark
Bottom tab bar: 56px + safe-area-inset-bottom, fixed, 4 tabs, active-state accent bar 4px above label
Locale pill: 32px rounded-full pill, shows OTHER locale's code (EN on /he/, HE on /en/)
Form field: 48px tall, focus ring uses --zc-ring (accent yellow), error border uses --zc-destructive
Primary CTA: full-width, 48px tall, bg --zc-primary text --zc-primary-foreground, rounded-xl
Empty-state card: bg --zc-card, border 1px --zc-border, rounded-2xl, centered, max-is-md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author message bundles + Zod schemas + session helpers + admin helpers</name>
  <files>
    src/lib/schemas/displayName.ts
    src/lib/schemas/join.ts
    src/lib/auth/session.ts
    src/lib/auth/admin.ts
    messages/he.json
    messages/en.json
  </files>
  <read_first>
    - .planning/phases/01-foundation-schema-auth-rls/01-UI-SPEC.md §"Copywriting Contract" (full inventory — every string below must match exactly)
    - .planning/phases/01-foundation-schema-auth-rls/01-CONTEXT.md D-04, D-06, D-07..D-10 (admin bootstrap + display name policy)
    - .planning/phases/01-foundation-schema-auth-rls/01-RESEARCH.md §"Code Examples — lib/auth/session.ts" + §"Code Examples — actions/join.ts" (joinSchema)
    - CLAUDE.md §"What NOT to Use" (getSession is forbidden)
    - src/lib/supabase/server.ts (just read interface, do not re-author)
    - src/types/supabase.ts (Database type)
  </read_first>
  <action>
    1. Author `messages/he.json` with EVERY string from UI-SPEC §"Copywriting Contract" — keys grouped by surface:

    ```json
    {
      "common": {
        "loading": "טוען...",
        "errorGeneric": "⚠ משהו השתבש. נסו שוב בעוד רגע.",
        "required": "⚠ שדה חובה.",
        "backToApp": "← חזרה לאפליקציה"
      },
      "wordmark": {
        "primary": "משחקי זערור",
        "secondary": "Zarur Cup"
      },
      "localePill": {
        "switchTo": "EN",
        "ariaLabel": "עברו לאנגלית"
      },
      "tabs": {
        "matches": "משחקים",
        "bracket": "ברקט",
        "leaderboard": "לוח",
        "me": "אני"
      },
      "home": {
        "redirectToMatches": "מעבר למשחקים..."
      },
      "join": {
        "pageHeading": "הצטרפו למשחקי זערור",
        "subheading": "בחרו שם שיופיע בלוח התוצאות, והכניסו את הקוד המשפחתי.",
        "nameLabel": "שם",
        "nameHelper": "2 עד 24 תווים — אותיות עברית, אנגלית, ספרות ורווחים.",
        "namePlaceholder": "למשל: דני",
        "codeLabel": "קוד הזמנה",
        "codeHelper": "קוד משפחתי שקיבלת בוואטסאפ.",
        "submitIdle": "הצטרפו",
        "submitLoading": "מצטרפים...",
        "errors": {
          "invalid_code": "⚠ הקוד שגוי. בדקו שוב בוואטסאפ המשפחתי.",
          "display_name_taken": "⚠ השם הזה כבר תפוס — נסו שם אחר.",
          "name_length": "⚠ השם חייב להיות בין 2 ל־24 תווים.",
          "name_chars": "⚠ השם יכול להכיל רק אותיות עברית/אנגלית, ספרות ורווחים.",
          "auth_failed": "⚠ משהו השתבש. נסו שוב בעוד רגע."
        }
      },
      "empty": {
        "matches": {
          "heading": "המשחקים פותחים ב-11 ביוני",
          "body": "החזרו ביום הפתיחה כדי להגיש את הניחושים שלכם."
        },
        "bracket": {
          "heading": "הברקט פותח לקראת שלב הנוקאאוט",
          "body": "תוכלו לסמן את ההמשך החל מ-27 ביוני."
        },
        "leaderboard": {
          "heading": "לוח התוצאות פותח עם הפתיחה",
          "body": "הניקוד יופיע אוטומטית אחרי המשחק הראשון."
        }
      },
      "me": {
        "joinedAt": "הצטרפת ב־{joined_at_local}",
        "localeLabel": "שפה"
      },
      "notFound": {
        "heading": "הדף לא נמצא",
        "body": "אולי שגיאת הקלדה בקישור?",
        "backLink": "חזרה למשחקים"
      }
    }
    ```

    2. Author `messages/en.json` mirroring every key with English values from UI-SPEC §"Copywriting Contract":

    ```json
    {
      "common": {
        "loading": "Loading...",
        "errorGeneric": "⚠ Something went wrong. Try again in a moment.",
        "required": "⚠ Required.",
        "backToApp": "← Back to app"
      },
      "wordmark": {
        "primary": "Zarur Cup",
        "secondary": "משחקי זערור"
      },
      "localePill": {
        "switchTo": "HE",
        "ariaLabel": "Switch to Hebrew"
      },
      "tabs": {
        "matches": "Matches",
        "bracket": "Bracket",
        "leaderboard": "Leaderboard",
        "me": "Me"
      },
      "home": {
        "redirectToMatches": "Redirecting to matches..."
      },
      "join": {
        "pageHeading": "Join Zarur Cup",
        "subheading": "Pick the name you want on the leaderboard, then enter the family invite code.",
        "nameLabel": "Your name",
        "nameHelper": "2-24 characters - Hebrew, Latin letters, digits, and spaces.",
        "namePlaceholder": "e.g. Dani",
        "codeLabel": "Invite code",
        "codeHelper": "The family code you received over WhatsApp.",
        "submitIdle": "Join the Pool",
        "submitLoading": "Joining…",
        "errors": {
          "invalid_code": "⚠ Wrong code. Check the family WhatsApp.",
          "display_name_taken": "⚠ Someone already took that name — try another.",
          "name_length": "⚠ Name must be 2-24 characters.",
          "name_chars": "⚠ Name can only contain Hebrew/Latin letters, digits, and spaces.",
          "auth_failed": "⚠ Something went wrong. Try again in a moment."
        }
      },
      "empty": {
        "matches": {
          "heading": "Matches open June 11",
          "body": "Come back on opening day to lock in your picks."
        },
        "bracket": {
          "heading": "Bracket opens before knockouts",
          "body": "You'll fill in your picks starting June 27."
        },
        "leaderboard": {
          "heading": "Leaderboard goes live on opening day",
          "body": "Standings will appear automatically after the first match."
        }
      },
      "me": {
        "joinedAt": "Joined {joined_at_local}",
        "localeLabel": "Language"
      },
      "notFound": {
        "heading": "Page not found",
        "body": "The link may have a typo.",
        "backLink": "Back to Matches"
      }
    }
    ```

    3. Write `src/lib/schemas/displayName.ts` per D-07 (length 2-24, allowed chars Hebrew + Latin + digits + space):

    ```typescript
    import { z } from 'zod';

    // D-07: 2-24 chars; Hebrew letters, Latin letters, digits, and ASCII space.
    // The trim + NFC normalization happens elsewhere; this regex validates the post-trim shape.
    // \p{L} matches all Unicode letters (Hebrew + Latin both); \d matches digits.
    export const displayNameSchema = z
      .string()
      .trim()
      .min(2, 'name_length')
      .max(24, 'name_length')
      .regex(/^[\p{L}\d ]+$/u, 'name_chars')
      .refine((s) => !/^\s|\s$/.test(s), 'name_chars');

    export type DisplayName = z.infer<typeof displayNameSchema>;

    // Shared normalization helper — used by both the action (for admin-name match)
    // and by Postgres (via the display_name_normalized generated column).
    export function normalizeDisplayName(s: string): string {
      return s.trim().normalize('NFC').toLowerCase();
    }
    ```

    4. Write `src/lib/schemas/join.ts`:

    ```typescript
    import { z } from 'zod';
    import { displayNameSchema } from './displayName';

    export const joinSchema = z.object({
      invite_code: z.string().trim().min(1, 'invalid_code'),
      display_name: displayNameSchema,
    });

    export type JoinInput = z.infer<typeof joinSchema>;
    ```

    5. Write `src/lib/auth/session.ts` per RESEARCH §"Code Examples — lib/auth/session.ts" and using `getClaims()` per CLAUDE.md (never `getSession()`):

    ```typescript
    import 'server-only';
    import { redirect } from 'next/navigation';
    import { createClient } from '@/lib/supabase/server';
    import type { Database } from '@/types/supabase';

    export type Member = Database['public']['Tables']['profiles']['Row'];

    export async function getCurrentMember(): Promise<Member | null> {
      const supabase = await createClient();
      // CRITICAL: getClaims(), NOT getSession() — per CLAUDE.md PITFALLS.
      // getSession() trusts cookies; getClaims() validates JWT against JWKS.
      const { data } = await supabase.auth.getClaims();
      if (!data?.claims) return null;

      const userId = data.claims.sub;
      const { data: profile } = await supabase
        .from('profiles')
        .select('*')
        .eq('user_id', userId)
        .maybeSingle();

      return profile ?? null;
    }

    export async function requireMember(locale: string): Promise<Member> {
      const member = await getCurrentMember();
      if (!member) redirect(`/${locale}/join`);
      return member;
    }

    export async function requireAdmin(): Promise<Member> {
      const member = await getCurrentMember();
      if (!member) redirect('/'); // root middleware sends to /[locale]/join
      if (!member.is_admin) redirect('/admin/403');
      return member;
    }
    ```

    6. Write `src/lib/auth/admin.ts` — pure helper for the join action to determine `is_admin`:

    ```typescript
    import 'server-only';
    import { normalizeDisplayName } from '@/lib/schemas/displayName';

    /**
     * D-04: Admin identity is established by env-var display-name match.
     * Returns true iff ADMIN_DISPLAY_NAME is set AND the normalized input matches.
     * Normalization (trim + NFC + lower) MUST match what the DB generated column does
     * so the admin gate is deterministic across re-joins.
     */
    export function isAdminDisplayName(displayName: string): boolean {
      const adminName = process.env.ADMIN_DISPLAY_NAME;
      if (!adminName) return false;
      return normalizeDisplayName(displayName) === normalizeDisplayName(adminName);
    }
    ```
  </action>
  <verify>
    <automated>
      cd "/Users/zekez/Documents/Claude OS/zarur-cup" && \
      test -f messages/he.json && test -f messages/en.json && \
      python3 -c "import json; json.load(open('messages/he.json'))" && \
      python3 -c "import json; json.load(open('messages/en.json'))" && \
      grep -q "הצטרפו" messages/he.json && \
      grep -q "Join the Pool" messages/en.json && \
      grep -q "invalid_code" messages/he.json && \
      grep -q "invalid_code" messages/en.json && \
      grep -q "Matches open June 11" messages/en.json && \
      grep -q "11 ביוני" messages/he.json && \
      test -f src/lib/schemas/displayName.ts && \
      test -f src/lib/schemas/join.ts && \
      test -f src/lib/auth/session.ts && \
      test -f src/lib/auth/admin.ts && \
      grep -q "getClaims" src/lib/auth/session.ts && \
      ! grep -q "getSession" src/lib/auth/session.ts && \
      grep -q "'server-only'" src/lib/auth/session.ts && \
      grep -q "'server-only'" src/lib/auth/admin.ts && \
      grep -q "ADMIN_DISPLAY_NAME" src/lib/auth/admin.ts && \
      grep -q "normalize" src/lib/schemas/displayName.ts && \
      grep -q "redirect" src/lib/auth/session.ts && \
      echo "VERIFY PASS"
    </automated>
  </verify>
  <done>
    - Both message bundles are valid JSON, parsed by python -c json.load
    - All UI-SPEC §"Copywriting Contract" strings are present (spot-checked: invite-code error, "11 ביוני", "Join the Pool")
    - Zod schemas exist for displayName + join
    - Session helpers use `getClaims()` and never `getSession()`
    - Admin helper normalizes via the shared `normalizeDisplayName` function
  </done>
</task>

<task type="auto">
  <name>Task 2: joinPool Server Action + JoinForm client component + /[locale]/join page</name>
  <files>
    src/app/actions/join.ts
    src/app/actions/locale.ts
    src/app/[locale]/join/page.tsx
    src/components/auth/JoinForm.client.tsx
    src/components/ui/FormField.tsx
  </files>
  <read_first>
    - .planning/phases/01-foundation-schema-auth-rls/01-RESEARCH.md §"Code Examples — actions/join.ts" (the canonical pattern; DO follow it)
    - .planning/phases/01-foundation-schema-auth-rls/01-UI-SPEC.md §5 "/join Page Hero + Form Container" + §6 "Form Field" + "Copywriting — error states"
    - .planning/phases/01-foundation-schema-auth-rls/01-CONTEXT.md D-01..D-03 (invite-code UX), D-07..D-10 (display name), D-17 (locale toggle persists to profiles.locale)
    - src/lib/schemas/join.ts (just authored — read interface)
    - src/lib/auth/admin.ts (just authored)
    - messages/he.json (key names for translations)
    - CLAUDE.md §"Auth: The Invite-Code-Without-Email Question"
  </read_first>
  <action>
    1. Write `src/app/actions/join.ts` per RESEARCH canonical pattern, adjusted for our schemas + admin bootstrap:

    ```typescript
    'use server';

    import { redirect } from 'next/navigation';
    import { cookies } from 'next/headers';
    import { getLocale } from 'next-intl/server';
    import { joinSchema } from '@/lib/schemas/join';
    import { isAdminDisplayName } from '@/lib/auth/admin';
    import { createClient } from '@/lib/supabase/server';

    export type JoinState = {
      error?:
        | 'validation_failed'
        | 'invalid_code'
        | 'auth_failed'
        | 'display_name_taken'
        | 'profile_failed';
      issues?: Array<{ path: string; code: string }>;
    } | null;

    export async function joinPool(
      _prev: JoinState,
      formData: FormData,
    ): Promise<JoinState> {
      const locale = await getLocale();

      // 1. Validate input.
      const parsed = joinSchema.safeParse({
        invite_code: formData.get('invite_code'),
        display_name: formData.get('display_name'),
      });
      if (!parsed.success) {
        return {
          error: 'validation_failed',
          issues: parsed.error.issues.map((i) => ({
            path: i.path.join('.'),
            code: i.message,
          })),
        };
      }
      const { invite_code, display_name } = parsed.data;

      // 2. Invite-code gate (D-01: exact string equality, case-sensitive, trimmed by Zod).
      //    D-12: NO cutoff / no admin approval — anyone with the code can join at any time.
      if (invite_code !== process.env.INVITE_CODE) {
        return { error: 'invalid_code' };
      }

      // 3. Sign in anonymously (Supabase rate-limits to 30/hr/IP — AUTH-07 per D-02).
      const supabase = await createClient();
      const { data: signIn, error: signInErr } = await supabase.auth.signInAnonymously();
      if (signInErr || !signIn?.user) {
        return { error: 'auth_failed' };
      }

      // 4. Determine admin status from env-var display-name match (D-04).
      const isAdmin = isAdminDisplayName(display_name);

      // 5. Insert profile row keyed on auth.uid().
      // The display_name_normalized generated column will catch uniqueness conflicts.
      const { error: profileErr } = await supabase.from('profiles').insert({
        user_id: signIn.user.id,
        display_name,         // preserves casing/composition
        locale,
        is_admin: isAdmin,
      });

      if (profileErr) {
        // 23505 = unique_violation on profiles_display_name_normalized_uniq
        if (profileErr.code === '23505') {
          // Sign out the anonymous user we just created so the next attempt is clean.
          await supabase.auth.signOut();
          return { error: 'display_name_taken' };
        }
        await supabase.auth.signOut();
        return { error: 'profile_failed' };
      }

      // 6. Set NEXT_LOCALE cookie to the chosen locale (matches what the user already picked via URL).
      // next-intl middleware reads this on subsequent requests.
      (await cookies()).set('NEXT_LOCALE', locale, {
        path: '/',
        maxAge: 60 * 60 * 24 * 365,
        sameSite: 'lax',
      });

      // 7. Redirect to localized landing — Phase 2 will land on /matches; Phase 1 lands on /.
      redirect(`/${locale}`);
    }
    ```

    2. Write `src/app/actions/locale.ts` — Server Action used by the locale pill to persist `profiles.locale` for signed-in users (D-17). The pill itself still navigates via next-intl `<Link>`, but on click for signed-in users we also fire this action.

    ```typescript
    'use server';

    import { revalidatePath } from 'next/cache';
    import { cookies } from 'next/headers';
    import { createClient } from '@/lib/supabase/server';

    export async function updateLocaleForCurrentUser(locale: 'he' | 'en'): Promise<void> {
      // Always update the cookie (works for both signed-in and signed-out users).
      (await cookies()).set('NEXT_LOCALE', locale, {
        path: '/',
        maxAge: 60 * 60 * 24 * 365,
        sameSite: 'lax',
      });

      // If signed in, also persist to profiles.locale.
      const supabase = await createClient();
      const { data } = await supabase.auth.getClaims();
      if (data?.claims) {
        await supabase
          .from('profiles')
          .update({ locale })
          .eq('user_id', data.claims.sub);
      }

      revalidatePath('/', 'layout');
    }
    ```

    3. Write `src/components/ui/FormField.tsx` per UI-SPEC §6:

    ```tsx
    type Props = {
      id: string;
      name: string;
      label: string;
      helper?: string;
      placeholder?: string;
      type?: 'text' | 'password';
      autoComplete?: string;
      required?: boolean;
      defaultValue?: string;
      error?: string;
    };

    export function FormField({
      id, name, label, helper, placeholder, type = 'text', autoComplete, required, defaultValue, error,
    }: Props) {
      const helperId = helper ? `${id}-helper` : undefined;
      const errorId = error ? `${id}-error` : undefined;
      const describedBy = [helperId, errorId].filter(Boolean).join(' ') || undefined;
      const borderClass = error
        ? 'border-[--zc-destructive]'
        : 'border-[--zc-border] focus-visible:border-[--zc-accent]';
      return (
        <div className="flex flex-col gap-2">
          <label htmlFor={id} className="text-sm font-bold text-[--zc-primary]">
            {label}
          </label>
          <input
            id={id}
            name={name}
            type={type}
            autoComplete={autoComplete}
            placeholder={placeholder}
            defaultValue={defaultValue}
            required={required}
            aria-describedby={describedBy}
            aria-invalid={!!error || undefined}
            className={`bs-12 is-full bg-white rounded-xl border-1 ${borderClass} ps-4 pe-4 text-base text-[--zc-primary] placeholder:text-[--zc-muted-foreground] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[--zc-ring] focus-visible:ring-offset-2 focus-visible:ring-offset-[--zc-card]`}
          />
          {helper && (
            <p id={helperId} className="text-sm text-[--zc-muted-foreground]">
              {helper}
            </p>
          )}
          {error && (
            <p id={errorId} className="text-sm text-[--zc-destructive]" aria-live="polite">
              {error}
            </p>
          )}
        </div>
      );
    }
    ```

    4. Write `src/components/auth/JoinForm.client.tsx` — client component using `useActionState`:

    ```tsx
    'use client';

    import { useActionState } from 'react';
    import { useFormStatus } from 'react-dom';
    import { useTranslations } from 'next-intl';
    import { joinPool, type JoinState } from '@/app/actions/join';
    import { FormField } from '@/components/ui/FormField';

    function SubmitButton() {
      const { pending } = useFormStatus();
      const t = useTranslations('join');
      return (
        <button
          type="submit"
          disabled={pending}
          aria-busy={pending}
          className="bs-12 is-full bg-[--zc-primary] text-[--zc-primary-foreground] rounded-xl font-bold text-base hover:bg-[#13325a] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[--zc-ring] focus-visible:ring-offset-2 focus-visible:ring-offset-[--zc-card] disabled:opacity-80"
        >
          {pending ? t('submitLoading') : t('submitIdle')}
        </button>
      );
    }

    export function JoinForm() {
      const t = useTranslations('join');
      const [state, formAction] = useActionState<JoinState, FormData>(joinPool, null);

      function errorMessageFor(field: 'display_name' | 'invite_code'): string | undefined {
        if (!state) return undefined;
        // Top-level errors apply to a known field
        if (field === 'invite_code' && state.error === 'invalid_code') return t('errors.invalid_code');
        if (field === 'display_name' && state.error === 'display_name_taken') return t('errors.display_name_taken');

        // Zod issues -> map by path + code
        if (state.error === 'validation_failed' && state.issues) {
          const match = state.issues.find((i) => i.path === field);
          if (match) {
            if (match.code === 'name_length') return t('errors.name_length');
            if (match.code === 'name_chars') return t('errors.name_chars');
          }
        }
        return undefined;
      }

      const formError =
        state?.error === 'auth_failed' || state?.error === 'profile_failed'
          ? t('errors.auth_failed')
          : undefined;

      return (
        <form action={formAction} className="flex flex-col gap-5">
          <FormField
            id="display_name"
            name="display_name"
            label={t('nameLabel')}
            helper={t('nameHelper')}
            placeholder={t('namePlaceholder')}
            autoComplete="username"
            required
            error={errorMessageFor('display_name')}
          />
          <FormField
            id="invite_code"
            name="invite_code"
            label={t('codeLabel')}
            helper={t('codeHelper')}
            autoComplete="off"
            required
            error={errorMessageFor('invite_code')}
          />
          {formError && (
            <p className="text-sm text-[--zc-destructive]" aria-live="polite">
              {formError}
            </p>
          )}
          <SubmitButton />
        </form>
      );
    }
    ```

    5. Write `src/app/[locale]/join/page.tsx` per UI-SPEC §5 (hero block + form container):

    ```tsx
    import { setRequestLocale, getTranslations } from 'next-intl/server';
    import { JoinForm } from '@/components/auth/JoinForm.client';
    import { getCurrentMember } from '@/lib/auth/session';
    import { redirect } from 'next/navigation';

    type Props = { params: Promise<{ locale: string }> };

    export default async function JoinPage({ params }: Props) {
      const { locale } = await params;
      setRequestLocale(locale);

      // If already signed in with a profile, send them home.
      const member = await getCurrentMember();
      if (member) redirect(`/${locale}`);

      const t = await getTranslations('join');
      const wordmarkT = await getTranslations('wordmark');

      return (
        <main className="mi-auto max-is-md ps-4 pe-4 md:ps-6 md:pe-6 pbs-12 pbe-12 md:pbs-16 md:pbe-16">
          <div className="text-center md:text-start mbe-8">
            <h1 className="text-3xl font-bold text-[--zc-primary]">{wordmarkT('primary')}</h1>
            <p className="text-sm text-[--zc-muted-foreground]" lang={locale === 'he' ? 'en' : 'he'}>
              {wordmarkT('secondary')}
            </p>
          </div>
          <section className="bg-[--zc-card] border-1 border-[--zc-border] rounded-2xl ps-6 pe-6 pbs-6 pbe-6">
            <h2 className="text-3xl font-bold text-[--zc-primary]">{t('pageHeading')}</h2>
            <p className="text-base text-[--zc-muted-foreground] mbs-2">{t('subheading')}</p>
            <div className="mbs-6">
              <JoinForm />
            </div>
          </section>
        </main>
      );
    }
    ```

    Implementation notes:
    - The `JoinForm` is a client component (`'use client'`) because `useActionState` is client-only.
    - The page itself is a server component that early-redirects signed-in users away from `/join`.
    - All text comes from `messages/{he,en}.json` — no hardcoded literals.
    - The sub-wordmark wraps in `<p lang>` for proper bidi behavior per UI-SPEC §"Focus / A11y Minimum" + I18N-06.
    - Hover hex `#13325a` is the 12% lighter navy — per UI-SPEC §5 — used as an arbitrary value because no Tailwind shade matches `#0a2540`.
    - DO NOT use any `pl-`, `pr-`, `ml-`, `mr-`, `text-left`, `text-right`, `flex-row-reverse` anywhere. FND-03 lint runs at commit.
  </action>
  <verify>
    <automated>
      cd "/Users/zekez/Documents/Claude OS/zarur-cup" && \
      test -f src/app/actions/join.ts && \
      test -f src/app/actions/locale.ts && \
      test -f src/app/[locale]/join/page.tsx && \
      test -f src/components/auth/JoinForm.client.tsx && \
      test -f src/components/ui/FormField.tsx && \
      grep -q "'use server'" src/app/actions/join.ts && \
      grep -q "signInAnonymously" src/app/actions/join.ts && \
      grep -q "process.env.INVITE_CODE" src/app/actions/join.ts && \
      grep -q "isAdminDisplayName" src/app/actions/join.ts && \
      grep -q "23505" src/app/actions/join.ts && \
      grep -q "'use client'" src/components/auth/JoinForm.client.tsx && \
      grep -q "useActionState" src/components/auth/JoinForm.client.tsx && \
      grep -q "useTranslations" src/components/auth/JoinForm.client.tsx && \
      ! grep -RnE '\b(p|m|border|rounded)[lr]-|\btext-(left|right)\b|\bflex-row-reverse\b' src/components/ src/app/[locale]/join/ src/app/actions/ && \
      npm run build > /tmp/zc-plan04t2.log 2>&1 && \
      echo "VERIFY PASS"
    </automated>
  </verify>
  <done>
    - joinPool Server Action exists with the 7-step canonical pattern
    - JoinForm uses useActionState + useFormStatus + useTranslations
    - All Zod issue codes map back to localized error messages
    - 23505 unique_violation triggers display_name_taken
    - Page early-redirects signed-in users away from /join
    - No physical Tailwind utilities in the new code
    - `npm run build` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Header + Wordmark + LocaleTogglePill + BottomTabBar + EmptyStateCard + placeholder pages + admin shell + 404</name>
  <files>
    src/components/layout/Header.tsx
    src/components/layout/Wordmark.tsx
    src/components/layout/LocaleTogglePill.client.tsx
    src/components/layout/BottomTabBar.tsx
    src/components/layout/EmptyStateCard.tsx
    src/app/[locale]/layout.tsx
    src/app/[locale]/page.tsx
    src/app/[locale]/matches/page.tsx
    src/app/[locale]/bracket/page.tsx
    src/app/[locale]/leaderboard/page.tsx
    src/app/[locale]/me/page.tsx
    src/app/[locale]/not-found.tsx
    src/app/admin/layout.tsx
    src/app/admin/page.tsx
    src/app/admin/403/page.tsx
  </files>
  <read_first>
    - .planning/phases/01-foundation-schema-auth-rls/01-UI-SPEC.md §"Per-Surface Component Contracts" (all 9 sub-sections — Header, Bottom Tab Bar, Locale Pill, Form Field, Empty State Card, Admin Shell, Error Pages, Form Container, Root layout)
    - .planning/phases/01-foundation-schema-auth-rls/01-CONTEXT.md D-05 (admin unlocalized), D-06 (admin gate at layout), D-16 (4 tabs), D-17 (locale pill)
    - .planning/phases/01-foundation-schema-auth-rls/01-RESEARCH.md §"Code Examples — Header pill locale toggle" + §"lib/auth/session.ts" (requireAdmin)
    - src/lib/i18n/routing.ts (Link, usePathname, etc. — already in place)
    - src/lib/auth/session.ts (just authored — requireMember, requireAdmin)
    - src/app/[locale]/layout.tsx (existing from Plan 01 — will be updated here to include Header + BottomTabBar)
    - messages/he.json (key names)
  </read_first>
  <action>
    1. Write `src/components/layout/Wordmark.tsx` per UI-SPEC §2 + CONTEXT.md D-14 (text-only wordmark — Hebrew 'משחקי זערור' on /he/, Latin 'Zarur Cup' on /en/, no icon):

    ```tsx
    import { useTranslations } from 'next-intl';
    import { Link } from '@/lib/i18n/routing';

    export function Wordmark() {
      const t = useTranslations('wordmark');
      return (
        <Link href="/" className="text-xl font-bold text-[--zc-primary] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[--zc-ring] focus-visible:ring-offset-2 focus-visible:ring-offset-[--zc-card] rounded">
          {t('primary')}
        </Link>
      );
    }
    ```

    2. Write `src/components/layout/LocaleTogglePill.client.tsx` per UI-SPEC §4 + RESEARCH §"Code Examples":

    ```tsx
    'use client';

    import { useParams } from 'next/navigation';
    import { useTransition } from 'react';
    import { useTranslations } from 'next-intl';
    import { Link, usePathname } from '@/lib/i18n/routing';
    import { updateLocaleForCurrentUser } from '@/app/actions/locale';

    export function LocaleTogglePill() {
      const t = useTranslations('localePill');
      const pathname = usePathname();
      const { locale } = useParams<{ locale: 'he' | 'en' }>();
      const otherLocale = locale === 'he' ? 'en' : 'he';
      const [, startTransition] = useTransition();

      function handleClick() {
        // Fire-and-forget — the Link's navigation handles the visible swap;
        // this just persists profiles.locale for signed-in users.
        startTransition(() => {
          updateLocaleForCurrentUser(otherLocale).catch(() => {});
        });
      }

      return (
        <Link
          href={pathname}
          locale={otherLocale}
          onClick={handleClick}
          aria-label={t('ariaLabel')}
          className="bs-8 inline-flex items-center justify-center rounded-full border-1 border-[--zc-border] bg-transparent ps-3 pe-3 text-sm font-bold text-[--zc-primary] hover:bg-[--zc-muted] hover:border-[--zc-accent] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[--zc-ring] focus-visible:ring-offset-2 focus-visible:ring-offset-[--zc-card]"
        >
          {t('switchTo')}
        </Link>
      );
    }
    ```

    3. Write `src/components/layout/Header.tsx` per UI-SPEC §2:

    ```tsx
    import { Wordmark } from './Wordmark';
    import { LocaleTogglePill } from './LocaleTogglePill.client';

    export function Header() {
      return (
        <header className="fixed inset-bs-0 inset-i-0 z-40 bs-14 bg-[--zc-card] border-b-1 border-[--zc-border]">
          <div className="bs-full flex items-center justify-between ps-4 pe-4">
            <Wordmark />
            <LocaleTogglePill />
          </div>
        </header>
      );
    }
    ```

    4. Write `src/components/layout/BottomTabBar.tsx` per UI-SPEC §3. Order in DOM: Matches -> Bracket -> Leaderboard -> Me. Active state via `aria-current="page"` + visible accent bar.

    ```tsx
    import { useTranslations } from 'next-intl';
    import { Link } from '@/lib/i18n/routing';
    import { headers } from 'next/headers';

    type Tab = {
      key: 'matches' | 'bracket' | 'leaderboard' | 'me';
      href: '/matches' | '/bracket' | '/leaderboard' | '/me';
    };

    const TABS: Tab[] = [
      { key: 'matches', href: '/matches' },
      { key: 'bracket', href: '/bracket' },
      { key: 'leaderboard', href: '/leaderboard' },
      { key: 'me', href: '/me' },
    ];

    export async function BottomTabBar() {
      const t = useTranslations('tabs');
      // Determine the active tab from the URL pathname (server-side via header).
      const headersList = await headers();
      const pathname = headersList.get('x-pathname') ?? '';
      // The middleware writes the locale-stripped path into x-pathname (see middleware update below).
      const activeKey = TABS.find((tab) => pathname.startsWith(tab.href))?.key ?? null;

      return (
        <nav
          aria-label="Primary"
          className="fixed inset-be-0 inset-i-0 z-40 bg-[--zc-card] border-t-1 border-[--zc-border] pbs-2 ps-2 pe-2"
          style={{ paddingBottomBlockEnd: 'calc(8px + env(safe-area-inset-bottom))' }}
        >
          <ul className="flex flex-row items-stretch justify-around">
            {TABS.map((tab) => {
              const active = tab.key === activeKey;
              return (
                <li key={tab.key} className="flex-1">
                  <Link
                    href={tab.href}
                    aria-current={active ? 'page' : undefined}
                    className="flex flex-col items-center justify-center bs-11 is-full focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[--zc-ring] focus-visible:ring-offset-2 focus-visible:ring-offset-[--zc-card]"
                  >
                    {active && <span className="bs-1 is-8 bg-[--zc-accent] mbe-1 rounded" aria-hidden />}
                    <span className={`text-sm ${active ? 'font-bold text-[--zc-primary]' : 'font-normal text-[--zc-muted-foreground]'}`}>
                      {t(tab.key)}
                    </span>
                  </Link>
                </li>
              );
            })}
          </ul>
        </nav>
      );
    }
    ```

    Also update `middleware.ts` to write `x-pathname` header (needed by the server tab bar). Edit the existing file:
    ```typescript
    // Inside middleware(), AFTER intlMiddleware runs and BEFORE refreshSupabaseSession:
    response.headers.set('x-pathname', request.nextUrl.pathname);
    ```

    5. Write `src/components/layout/EmptyStateCard.tsx` per UI-SPEC §7:

    ```tsx
    type Props = { heading: string; body: string };

    export function EmptyStateCard({ heading, body }: Props) {
      return (
        <section className="mi-auto max-is-md mbs-12 ps-6 pe-6 pbs-8 pbe-8 text-center bg-[--zc-card] border-1 border-[--zc-border] rounded-2xl">
          <h2 className="text-xl font-bold text-[--zc-primary]">{heading}</h2>
          <p className="text-base text-[--zc-muted-foreground] mbs-2">{body}</p>
        </section>
      );
    }
    ```

    6. Update `src/app/[locale]/layout.tsx` to include Header + BottomTabBar with proper top/bottom padding:

    Modify the existing file from Plan 01 — keep the font + dir + NextIntlProvider; ADD the Header + main wrapper + BottomTabBar:

    ```tsx
    // ... existing imports ...
    import { Header } from '@/components/layout/Header';
    import { BottomTabBar } from '@/components/layout/BottomTabBar';

    // ... inside LocaleLayout, replace <body>{children}</body> with: ...
    <body className={`${bodyFont} min-bs-dvh bg-[--zc-surface] text-[--zc-surface-foreground]`}>
      <NextIntlClientProvider messages={messages} locale={locale}>
        <Header />
        <main
          className="pbs-14"
          style={{ paddingBlockEnd: 'calc(56px + env(safe-area-inset-bottom))' }}
        >
          {children}
        </main>
        <BottomTabBar />
      </NextIntlClientProvider>
    </body>
    ```

    7. Rewrite `src/app/[locale]/page.tsx` to redirect to /matches once we have a member, or to /join otherwise:

    ```tsx
    import { setRequestLocale } from 'next-intl/server';
    import { redirect } from 'next/navigation';
    import { getCurrentMember } from '@/lib/auth/session';

    type Props = { params: Promise<{ locale: string }> };

    export default async function HomePage({ params }: Props) {
      const { locale } = await params;
      setRequestLocale(locale);
      const member = await getCurrentMember();
      if (!member) redirect(`/${locale}/join`);
      redirect(`/${locale}/matches`);
    }
    ```

    8. Write the four tab pages. Each calls `requireMember()` (so the bottom-tab placeholder doesn't show to non-members) and renders an EmptyStateCard.

    `src/app/[locale]/matches/page.tsx`:
    ```tsx
    import { setRequestLocale, getTranslations } from 'next-intl/server';
    import { requireMember } from '@/lib/auth/session';
    import { EmptyStateCard } from '@/components/layout/EmptyStateCard';

    type Props = { params: Promise<{ locale: string }> };

    export default async function MatchesPage({ params }: Props) {
      const { locale } = await params;
      setRequestLocale(locale);
      await requireMember(locale);
      const t = await getTranslations('empty.matches');
      return <EmptyStateCard heading={t('heading')} body={t('body')} />;
    }
    ```

    `src/app/[locale]/bracket/page.tsx`:
    ```tsx
    import { setRequestLocale, getTranslations } from 'next-intl/server';
    import { requireMember } from '@/lib/auth/session';
    import { EmptyStateCard } from '@/components/layout/EmptyStateCard';

    type Props = { params: Promise<{ locale: string }> };

    export default async function BracketPage({ params }: Props) {
      const { locale } = await params;
      setRequestLocale(locale);
      await requireMember(locale);
      const t = await getTranslations('empty.bracket');
      return <EmptyStateCard heading={t('heading')} body={t('body')} />;
    }
    ```

    `src/app/[locale]/leaderboard/page.tsx`: same shape with `empty.leaderboard`.

    `src/app/[locale]/me/page.tsx` — slightly different; shows the actual display name + joined-at + locale:
    ```tsx
    import { setRequestLocale, getTranslations } from 'next-intl/server';
    import { requireMember } from '@/lib/auth/session';

    type Props = { params: Promise<{ locale: string }> };

    export default async function MePage({ params }: Props) {
      const { locale } = await params;
      setRequestLocale(locale);
      const member = await requireMember(locale);
      const t = await getTranslations('me');

      const joinedAtLocal = new Intl.DateTimeFormat(locale === 'he' ? 'he-IL' : 'en-US', {
        dateStyle: 'long',
      }).format(new Date(member.joined_at));

      return (
        <section className="mi-auto max-is-md mbs-12 ps-6 pe-6 pbs-8 pbe-8 bg-[--zc-card] border-1 border-[--zc-border] rounded-2xl">
          <h2 className="text-xl font-bold text-[--zc-primary]">{member.display_name}</h2>
          <p className="text-sm text-[--zc-muted-foreground] mbs-2">
            {t('joinedAt', { joined_at_local: joinedAtLocal })}
          </p>
          <p className="text-sm text-[--zc-muted-foreground] mbs-1">
            {t('localeLabel')}: {member.locale === 'he' ? 'עברית' : 'English'}
          </p>
        </section>
      );
    }
    ```

    (W4: the `<p>` for `t('localeLabel')` uses the `me.localeLabel` string already present in messages/{he,en}.json — see Task 1 — so no message-key bundle has dead keys; cited UI-SPEC §Me-page.)

    9. Write `src/app/[locale]/not-found.tsx` per UI-SPEC §9:
    ```tsx
    import { getTranslations } from 'next-intl/server';
    import { Link } from '@/lib/i18n/routing';

    export default async function LocaleNotFound() {
      const t = await getTranslations('notFound');
      return (
        <section className="mi-auto max-is-md mbs-12 ps-6 pe-6 pbs-8 pbe-8 text-center bg-[--zc-card] border-1 border-[--zc-border] rounded-2xl">
          <h2 className="text-xl font-bold text-[--zc-primary]">{t('heading')}</h2>
          <p className="text-base text-[--zc-muted-foreground] mbs-2">{t('body')}</p>
          <Link href="/matches" className="text-sm text-[--zc-primary] underline mbs-4 inline-block">
            ← {t('backLink')}
          </Link>
        </section>
      );
    }
    ```

    10. Write `src/app/admin/layout.tsx` — server-gated admin shell per D-05 + D-06:
    ```tsx
    import { Inter } from 'next/font/google';
    import { requireAdmin } from '@/lib/auth/session';
    import { Link } from '@/lib/i18n/routing';

    const inter = Inter({ subsets: ['latin'], weight: ['400', '700'], variable: '--font-inter' });

    export default async function AdminLayout({ children }: { children: React.ReactNode }) {
      // Gate runs BEFORE rendering children. Non-admins are redirected.
      await requireAdmin();
      return (
        <html lang="en" dir="ltr" className={inter.variable}>
          <body className="font-inter min-bs-dvh bg-[--zc-surface] text-[--zc-surface-foreground]">
            <header className="flex items-center justify-between ps-4 pe-4 bs-14 border-b-1 border-[--zc-border] bg-[--zc-card]">
              <span className="text-xl font-bold text-[--zc-primary]">Admin</span>
              <a href="/" className="text-sm text-[--zc-primary] underline">← Back to app</a>
            </header>
            <main>{children}</main>
          </body>
        </html>
      );
    }
    ```

    11. Write `src/app/admin/page.tsx`:
    ```tsx
    export default function AdminHome() {
      return (
        <section className="mi-auto max-is-md mbs-12 ps-6 pe-6 pbs-8 pbe-8 bg-[--zc-card] border-1 border-[--zc-border] rounded-2xl text-center">
          <h1 className="text-3xl font-bold text-[--zc-primary]">Admin</h1>
          <p className="text-base text-[--zc-muted-foreground] mbs-2">
            No actions available yet. Admin pages light up in Phase 2.
          </p>
        </section>
      );
    }
    ```

    12. Write `src/app/admin/403/page.tsx` — important: this page must NOT call `requireAdmin()` (which would loop). It's the destination for non-admin signed-in users.

    ```tsx
    export default function Admin403() {
      return (
        <html lang="en" dir="ltr">
          <body className="font-inter min-bs-dvh bg-[--zc-surface] text-[--zc-surface-foreground]">
            <section className="mi-auto max-is-md mbs-12 ps-6 pe-6 pbs-8 pbe-8 bg-[--zc-card] border-1 border-[--zc-border] rounded-2xl text-center">
              <h1 className="text-3xl font-bold text-[--zc-destructive]">Admin only</h1>
              <p className="text-base text-[--zc-muted-foreground] mbs-2">
                This area is reserved for the tournament admin.
              </p>
              <a href="/" className="text-sm text-[--zc-primary] underline mbs-4 inline-block">
                ← Back to app
              </a>
            </section>
          </body>
        </html>
      );
    }
    ```

    Wait — `src/app/admin/403/page.tsx` would inherit the `admin/layout.tsx` and trip its `requireAdmin()` redirect to `/admin/403`, looping forever. Fix: place 403 OUTSIDE the admin layout. Use a route group:
    - Move admin pages into `src/app/admin/(protected)/layout.tsx` + `src/app/admin/(protected)/page.tsx`
    - Keep `src/app/admin/403/page.tsx` outside, with its own minimal layout (no gate).

    Reorganize:
    - `src/app/admin/layout.tsx` (outer; minimal Inter font + html/body shell; NO auth gate)
    - `src/app/admin/(protected)/layout.tsx` (inner; calls `requireAdmin()`)
    - `src/app/admin/(protected)/page.tsx` (Admin home)
    - `src/app/admin/403/page.tsx` (inside outer layout but OUTSIDE protected)

    Final structure:
    ```tsx
    // src/app/admin/layout.tsx (OUTER — no gate)
    import { Inter } from 'next/font/google';
    const inter = Inter({ subsets: ['latin'], weight: ['400', '700'], variable: '--font-inter' });
    export default function AdminOuterLayout({ children }: { children: React.ReactNode }) {
      return (
        <html lang="en" dir="ltr" className={inter.variable}>
          <body className="font-inter min-bs-dvh bg-[--zc-surface] text-[--zc-surface-foreground]">
            {children}
          </body>
        </html>
      );
    }
    ```

    ```tsx
    // src/app/admin/(protected)/layout.tsx (INNER — gate)
    import { requireAdmin } from '@/lib/auth/session';
    export default async function AdminProtectedLayout({ children }: { children: React.ReactNode }) {
      await requireAdmin();
      return (
        <>
          <header className="flex items-center justify-between ps-4 pe-4 bs-14 border-b-1 border-[--zc-border] bg-[--zc-card]">
            <span className="text-xl font-bold text-[--zc-primary]">Admin</span>
            <a href="/" className="text-sm text-[--zc-primary] underline">← Back to app</a>
          </header>
          <main>{children}</main>
        </>
      );
    }
    ```

    ```tsx
    // src/app/admin/(protected)/page.tsx
    export default function AdminHome() { ... }   // same as before
    ```

    ```tsx
    // src/app/admin/403/page.tsx (no inner gate — relies on outer layout only)
    export default function Admin403() { ... }   // contents per above, but without the html/body wrapper since outer layout supplies them
    ```

    Adjust the `requireAdmin()` redirect in `src/lib/auth/session.ts` to `/admin/403` (already specified above — confirm).

    Implementation notes:
    - The header pill is wrapped in `<Link locale={otherLocale}>` from next-intl, which natively handles the path swap (e.g. `/he/me` -> `/en/me`).
    - The `flex-row` order auto-flips via `<html dir>` per UI-SPEC §"Token-Theming for RTL." DO NOT use `flex-row-reverse`.
    - The `me` page formats `joined_at` via native `Intl.DateTimeFormat` per I18N-07.
    - Sub-wordmark on the `/join` hero uses `<p lang="<other>">` for bidi correctness (already in Task 2).
    - The `EmptyStateCard` heading is `<h2>` (since the page-level heading is implicit in the bottom-tab context); per UI-SPEC §"Focus / A11y Minimum" — heading hierarchy is per-page, no skip levels.
    - DO NOT use any physical Tailwind utility. Verified by `npm run lint:rtl`.
  </action>
  <verify>
    <automated>
      cd "/Users/zekez/Documents/Claude OS/zarur-cup" && \
      test -f src/components/layout/Header.tsx && \
      test -f src/components/layout/Wordmark.tsx && \
      test -f src/components/layout/LocaleTogglePill.client.tsx && \
      test -f src/components/layout/BottomTabBar.tsx && \
      test -f src/components/layout/EmptyStateCard.tsx && \
      test -f src/app/[locale]/matches/page.tsx && \
      test -f src/app/[locale]/bracket/page.tsx && \
      test -f src/app/[locale]/leaderboard/page.tsx && \
      test -f src/app/[locale]/me/page.tsx && \
      test -f src/app/[locale]/not-found.tsx && \
      test -f src/app/admin/layout.tsx && \
      test -f "src/app/admin/(protected)/layout.tsx" && \
      test -f "src/app/admin/(protected)/page.tsx" && \
      test -f src/app/admin/403/page.tsx && \
      grep -q "'use client'" src/components/layout/LocaleTogglePill.client.tsx && \
      grep -q "requireAdmin" "src/app/admin/(protected)/layout.tsx" && \
      ! grep -q "requireAdmin" src/app/admin/layout.tsx && \
      ! grep -q "requireAdmin" src/app/admin/403/page.tsx && \
      grep -q "requireMember" src/app/[locale]/matches/page.tsx && \
      grep -q "Intl.DateTimeFormat" src/app/[locale]/me/page.tsx && \
      grep -q "x-pathname" middleware.ts && \
      grep -q "Header" src/app/[locale]/layout.tsx && \
      grep -q "BottomTabBar" src/app/[locale]/layout.tsx && \
      ! grep -RnE '\b(p|m|border|rounded)[lr]-|\btext-(left|right)\b|\bflex-row-reverse\b' src/ && \
      npm run build > /tmp/zc-plan04t3.log 2>&1 && \
      # B7 build-snapshot: assert the CSS bundle exists and every logical-property utility
      # used by the chrome/UI shell compiled to a real CSS rule. CLAUDE.md is the authoritative
      # stack reference; if any assertion fails, fix tailwind config / CSS imports — DO NOT
      # rewrite the utility class. Fast-feedback for executor.
      ls .next/static/css/*.css > /dev/null 2>&1 || (echo 'B7 FAIL: no CSS file in .next/static/css/ after build.' && exit 1) && \
      grep -q 'block-size:' .next/static/css/*.css || (echo 'B7 FAIL: block-size: missing (bs-* did not compile). Check CLAUDE.md vs tailwind setup.' && exit 1) && \
      grep -q 'inline-size:' .next/static/css/*.css || (echo 'B7 FAIL: inline-size: missing (is-* did not compile).' && exit 1) && \
      grep -q 'margin-inline:' .next/static/css/*.css || (echo 'B7 FAIL: margin-inline: missing (mi-/ms-/me- did not compile).' && exit 1) && \
      grep -q 'padding-block-start:' .next/static/css/*.css || (echo 'B7 FAIL: padding-block-start: missing (pbs-* did not compile).' && exit 1) && \
      grep -q 'padding-block-end:' .next/static/css/*.css || (echo 'B7 FAIL: padding-block-end: missing (pbe-* did not compile).' && exit 1) && \
      grep -q 'margin-block-start:' .next/static/css/*.css || (echo 'B7 FAIL: margin-block-start: missing (mbs-* did not compile).' && exit 1) && \
      grep -q 'margin-block-end:' .next/static/css/*.css || (echo 'B7 FAIL: margin-block-end: missing (mbe-* did not compile).' && exit 1) && \
      grep -q 'inset-block-start:' .next/static/css/*.css || (echo 'B7 FAIL: inset-block-start: missing (inset-bs-* did not compile).' && exit 1) && \
      grep -q 'inset-inline:' .next/static/css/*.css || (echo 'B7 FAIL: inset-inline: missing (inset-i-* did not compile).' && exit 1) && \
      grep -q 'padding-inline-start:' .next/static/css/*.css || (echo 'B7 FAIL: padding-inline-start: missing (ps-* did not compile).' && exit 1) && \
      grep -q 'padding-inline-end:' .next/static/css/*.css || (echo 'B7 FAIL: padding-inline-end: missing (pe-* did not compile).' && exit 1) && \
      echo "VERIFY PASS"
    </automated>
  </verify>
  <done>
    - Header, Wordmark, LocaleTogglePill, BottomTabBar, EmptyStateCard all exist
    - All 4 tab pages exist; non-/me pages render EmptyStateCard; /me renders the member's display name + joined_at via Intl.DateTimeFormat
    - 404 page exists per locale
    - Admin shell: outer layout (no gate) + (protected) inner layout (gate) + 403 outside protected (no redirect loop)
    - Middleware now writes `x-pathname` header for the server bottom tab bar
    - `npm run build` passes; `npm run lint:rtl` returns no violations
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end UX verification on real browser (RTL + LTR + session + admin gate)</name>
  <what-built>Full Phase 1 user-facing surface: join page, session-aware redirects, bilingual chrome, admin gate, locale toggle.</what-built>
  <how-to-verify>
    Set `INVITE_CODE=test-2026` and `ADMIN_DISPLAY_NAME=Zeke` in `.env.local`. Restart `npm run dev`. Open a fresh incognito window.

    SCENARIO A — Hebrew default + invalid code:
    1. Visit http://localhost:3000/. Expect redirect to /he/join (Hebrew route + join page because no session).
    2. Page renders RTL: wordmark "משחקי זערור" on visual-right, locale pill "EN" on visual-left.
    3. Submit with name "test" and code "WRONG". Expect inline error: "⚠ הקוד שגוי. בדקו שוב בוואטסאפ המשפחתי."

    SCENARIO B — Successful join + session persist + bottom tabs + me page:
    4. Submit with name "Dani" and code "test-2026". Expect redirect to /he/matches.
    5. Bottom tab bar shows 4 tabs in Hebrew, ordered אני · לוח · ברקט · משחקים (visually RTL — Matches on visual-right since it's first in DOM order under RTL).
    6. The Matches tab is active (accent bar above label, label bold).
    7. Tap "אני" (Me) tab. Page shows "Dani" as the heading and "הצטרפת ב־<localized date>".
    8. Refresh the browser. Still signed in. Tab on Me shows Dani.
    9. Close the tab and reopen http://localhost:3000/ — still signed in (anonymous session cookies persist).

    SCENARIO C — Locale toggle:
    10. Tap the "EN" pill in the header. URL changes to /en/me. Page renders LTR. Wordmark "Zarur Cup" is now visual-left, pill "HE" is visual-right.
    11. Tab labels are now "Matches · Bracket · Leaderboard · Me" left-to-right (DOM order, visual LTR).
    12. The same Me page shows Dani's name with "Joined <localized date>".
    13. Verify `profiles.locale` updated in Supabase dashboard: SELECT locale FROM profiles WHERE display_name = 'Dani'; — expect 'en'.

    SCENARIO D — Display-name conflict:
    14. Open ANOTHER incognito window. Visit /. Submit with name "dani" (lowercase) and the correct code.
    15. Expect inline error: "⚠ השם הזה כבר תפוס — נסו שם אחר." (because Dani vs dani normalize to the same value via the generated column.)

    SCENARIO E — Admin gate:
    16. In yet another incognito window. Submit name "Zeke" and code "test-2026" (Zeke matches ADMIN_DISPLAY_NAME).
    17. Confirm in Supabase dashboard: SELECT is_admin FROM profiles WHERE display_name = 'Zeke'; — expect true.
    18. While signed in as Zeke, visit http://localhost:3000/admin/. Expect admin page renders (English-only, no locale prefix, no bottom tab bar).
    19. In the original Dani window, visit http://localhost:3000/admin/. Expect redirect to /admin/403 — "Admin only" page.

    SCENARIO F — Bracket / Leaderboard placeholders:
    20. As Dani, tap "ברקט" tab. Page shows EmptyStateCard with the Hebrew heading "הברקט פותח לקראת שלב הנוקאאוט" — NOT 404.
    21. Tap "לוח". Page shows EmptyStateCard with the Hebrew leaderboard placeholder copy.

    SCENARIO G — Visible regression checks:
    22. Devtools Elements panel: `<html>` always has both `lang` and `dir` set. NO `flex-row-reverse` anywhere in CSS. Bottom tab bar respects `env(safe-area-inset-bottom)` (test by simulating iPhone with home indicator).
    23. No console errors. No hydration warnings.
  </how-to-verify>
  <resume-signal>Type "approved — all 7 scenarios pass" or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form -> Server Action | Untrusted POST body; Zod validates; INVITE_CODE compared server-side only |
| Auth cookies -> Server `getClaims()` | JWT-signed; validated against JWKS (not blindly trusted) |
| `/admin/*` route boundary | Public URL surface; gate at layout level (D-06) |
| `process.env.INVITE_CODE` / `process.env.ADMIN_DISPLAY_NAME` | Server-only env vars; never NEXT_PUBLIC_ prefix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure (HIGH) | `process.env.INVITE_CODE` leaking to client bundle | mitigate | Env var has no `NEXT_PUBLIC_` prefix → Next.js strips it from client bundles. Only referenced inside `'use server';` file (`src/app/actions/join.ts`). Verified by `grep -Rn "INVITE_CODE" src/ \| grep -v 'use server'` returning only the action file. |
| T-04-02 | Information Disclosure (HIGH) | Stale auth via `getSession()` | mitigate | All session checks use `getClaims()` (validates JWT signature). `grep -Rn "getSession" src/lib/auth/` returns nothing. (CLAUDE.md PITFALLS) |
| T-04-03 | Elevation of Privilege (HIGH) | UI-only admin gate | mitigate | `src/app/admin/(protected)/layout.tsx` calls `requireAdmin()` at the layout level (D-06). UI hiding is not the gate. (CONTEXT.md D-06) |
| T-04-04 | Spoofing (MEDIUM) | Two users grabbing same display name via race | mitigate | DB-level uniqueness via `display_name_normalized` generated column with unique index (Plan 02). Server Action catches `23505` and returns inline error. |
| T-04-05 | Spoofing (MEDIUM) | Admin display-name squat | accept | D-04: mitigation is "zekez joins first." Documented accepted risk; would require Supabase auth migration to defend further. |
| T-04-06 | Denial of Service (LOW) | Invite-code brute force | accept | D-02: Supabase's built-in 30 anon-signins/hr/IP cap is the only defense in v1. AUTH-07 reinterpreted accordingly. If abuse observed, add Cloudflare Turnstile post-launch (RESEARCH Pitfall 9). |
| T-04-07 | Tampering (MEDIUM) | XSS via display_name rendering | mitigate | (1) Zod regex `/^[\p{L}\d ]+$/u` rejects `< > / \ & ' " ; \``, emoji, control chars at validation. (2) React JSX escapes by default. (3) No `dangerouslySetInnerHTML` anywhere — verified by `grep -Rn dangerouslySetInnerHTML src/` returning nothing. (D-10) |
| T-04-08 | Information Disclosure (LOW) | Locale pill server-side `profiles.locale` update happens even before auth | accept | The action checks `getClaims()` first; signed-out users skip the update. Cookie still updates locally (which is correct for anon users). |
| T-04-09 | Tampering (MEDIUM) | Admin gate bypassed because 403 page sits inside protected layout (redirect loop) | mitigate | Route grouping: `src/app/admin/(protected)/` holds protected pages; `src/app/admin/403/` lives outside the route group so it doesn't inherit the gate. Verified by `grep requireAdmin src/app/admin/403/` returning nothing. |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run lint:rtl` exits 0
- Manual checkpoint scenarios A–G all pass
- `grep -Rn INVITE_CODE src/` matches only `src/app/actions/join.ts` (server-only)
- `grep -Rn getSession src/` returns nothing
- `grep -Rn dangerouslySetInnerHTML src/` returns nothing
- Supabase dashboard shows `profiles` rows for the test joins with correct `display_name`, `display_name_normalized`, `is_admin`, `locale`, `joined_at`
- `bash scripts/verify-rls-no-leak.sh` still passes (RLS not regressed by app changes)
</verification>

<success_criteria>
- ROADMAP success criterion #1: First-visit on `Accept-Language: he-IL` lands on `/he/` RTL; `Accept-Language: en-US` lands on `/en/` LTR; `<html dir>` is server-rendered (no FOUC).
- ROADMAP success criterion #2: User enters invite code + picks display name on `/he/join`, lands in a protected page (`/he/matches`), and session survives refresh and same-device return.
- All 22 CONTEXT.md decisions D-01..D-22 represented in the implementation (auth, admin, display-name policy, late-entrant, bilingual shell, locale toggle, schema, RLS).
- All 15 of this plan's requirement IDs (FND-02, FND-06, I18N-01..07, AUTH-01..07) addressed by concrete code.
- Bottom tab bar shows 4 tabs in DOM order; visual order auto-flips via `<html dir>`; active tab indicated by accent bar + bold label.
- `/admin/*` is admin-only at the layout level; non-admins land on `/admin/403`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-schema-auth-rls/01-04-SUMMARY.md` capturing:
- Confirmed scenarios A-G (from Task 4 checkpoint) pass or describe any deviations
- The locale pill cookie name and persistence behavior (NEXT_LOCALE; 1-year maxAge)
- Any deviation from UI-SPEC component specs (with rationale)
- Any deviation from RESEARCH Server Action shape (with rationale)
- Note about `(protected)` route group fix for the admin 403 redirect loop
- Note that `messages/he.json` and `messages/en.json` will receive a full Hebrew native-speaker review pass in Phase 2 (QA-03) — Phase 1's team-name review (DATA-04) already happened in Plan 03
</output>
