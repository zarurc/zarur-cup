---
phase: 02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scoring/league.ts
  - src/lib/scoring/props.ts
  - src/lib/scoring/sweepAndUpsert.ts
  - src/lib/schemas/prediction.ts
  - src/lib/schemas/result.ts
  - src/lib/schemas/propAnswer.ts
  - src/lib/schemas/propAuthoring.ts
  - src/lib/auth/adminReadClient.ts
autonomous: true
requirements:
  - SCR-01
  - SCR-02
  - SCR-04
  - SCR-05
  - SCR-06
  - ADM-02
  - PRP-04
must_haves:
  truths:
    - "scoreMatch(prediction, result) returns {points: 4, kind: 'exact'} for exact match; {points: 3, kind: 'goal-diff'} for non-zero same goal-diff; {points: 2, kind: 'winner'} for same sign; {points: 0, kind: 'miss'} otherwise (SCR-01)"
    - "scoreMatch ignores ET/penalty fields; uses only result_home_90min and result_away_90min (SCR-02)"
    - "scoreProp returns {points: pointsAtStake, kind: 'correct'} when normalized userAnswer matches normalized correctAnswer OR any alias; otherwise {points: 0, kind: 'miss'} (SCR-04)"
    - "All scoring functions are pure (no DB, no IO, no imports of Supabase clients)"
    - "sweepAndUpsert deletes stragglers then bulk-upserts on (user_id, source, ref_id) and revalidates 8 explicit paths (per-locale × per-page) (D-18 + Pitfall 6)"
    - "adminReadClient calls requireAdmin() then returns service-role client (Pitfall 10)"
    - "Zod schemas at prediction.ts (0-9 each), result.ts (admin input), propAnswer.ts (discriminated union by answer_type), propAuthoring.ts (admin author + grade) all exist and parse valid input"
  artifacts:
    - path: src/lib/scoring/league.ts
      provides: "scoreMatch + LeagueScore + LeagueKind types"
      contains: "export function scoreMatch"
    - path: src/lib/scoring/props.ts
      provides: "scoreProp + PropScore + PropAnswerType types"
      contains: "export function scoreProp"
    - path: src/lib/scoring/sweepAndUpsert.ts
      provides: "shared server-only helper for D-18 idempotent sweep"
      contains: "export async function sweepAndUpsert"
    - path: src/lib/auth/adminReadClient.ts
      provides: "service-role read client gated by requireAdmin"
      contains: "export async function adminReadClient"
    - path: src/lib/schemas/prediction.ts
      provides: "predictionSchema for client+server validation"
      contains: "export const predictionSchema"
    - path: src/lib/schemas/result.ts
      provides: "resultSchema for admin save"
      contains: "export const resultSchema"
    - path: src/lib/schemas/propAnswer.ts
      provides: "propAnswerSchema for user prop save"
      contains: "export const propAnswerSchema"
    - path: src/lib/schemas/propAuthoring.ts
      provides: "propAuthoringSchema + propGradingSchema for admin"
      contains: "export const propAuthoringSchema"
  key_links:
    - from: "src/lib/scoring/sweepAndUpsert.ts"
      to: "score_events"
      via: "supabase.from('score_events').upsert(rows, { onConflict: 'user_id,source,ref_id' })"
      pattern: "onConflict.*user_id.*source.*ref_id"
    - from: "src/lib/scoring/sweepAndUpsert.ts"
      to: "Phase 2 admin Server Actions (saveResult, gradeProp)"
      via: "module export"
      pattern: "export async function sweepAndUpsert"
    - from: "src/lib/auth/adminReadClient.ts"
      to: "requireAdmin + createServiceClient"
      via: "compose"
      pattern: "await requireAdmin"
---

<objective>
Author the pure-TypeScript scoring engine (`league.ts`, `props.ts`), the shared idempotent sweep helper (`sweepAndUpsert.ts` per D-18 + RESEARCH Pattern 5), the admin-RSC read helper (`adminReadClient.ts` per Pitfall 10 + RESEARCH Open Q3), and the four Zod schemas the Wave-2+ Server Actions will validate against. Everything in this plan is pure / non-UI — no React, no Tailwind, no Server Action calls from inside scoring libs.

Purpose: Phase 2 D-16 chose "Server Action over Postgres trigger" — the scoring math lives in TS. Centralizing in pure modules makes the math unit-testable, debug-friendly during live tournament operation, and dead-trivial to mirror in Phase 3 for the Bracket Mode source. Schemas in this plan are imported by Wave 2+ Server Actions; the helpers are imported by every admin write path.

Output: 8 new TypeScript modules under `src/lib/` ready to be imported by Wave 2+ Server Actions and admin RSCs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-CONTEXT.md
@.planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-RESEARCH.md
@.planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-PATTERNS.md
@src/lib/schemas/displayName.ts
@src/lib/schemas/join.ts
@src/lib/auth/session.ts
@src/lib/supabase/service.ts
@CLAUDE.md

<interfaces>
<!-- Established Phase 1 patterns this plan must mirror: -->

From src/lib/schemas/displayName.ts (Zod v4 pattern, NFC normalization helper):
```typescript
import { z } from 'zod';
export const displayNameSchema = z.string().trim().min(2,'name_length').max(24,'name_length').regex(/^[\p{L}\d ]+$/u, 'name_chars');
export type DisplayName = z.infer<typeof displayNameSchema>;
export function normalizeDisplayName(s: string): string {
  return s.trim().normalize('NFC').toLowerCase();
}
```

From src/lib/auth/session.ts:
```typescript
import 'server-only';
import { redirect } from 'next/navigation';
import type { Route } from 'next';
import { createClient } from '@/lib/supabase/server';
export async function requireAdmin(): Promise<Member> { /* ... */ }
```

From src/lib/supabase/service.ts:
```typescript
import 'server-only';
import { createClient as createSupabaseClient } from '@supabase/supabase-js';
export function createServiceClient() { /* ... */ }
```

From src/types/supabase.ts (regenerated in Plan 02-01):
```typescript
Database['public']['Tables']['score_events']['Row'] // { user_id, source, ref_id, points, kind, updated_at }
Database['public']['Tables']['score_events']['Insert'] // upsert payload
Database['public']['Tables']['predictions']['Row'] // { user_id, fixture_id, home_score, away_score, submitted_at }
Database['public']['Tables']['fixtures']['Row']['result_home_90min'] // number | null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author scoring/league.ts + scoring/props.ts (pure TS)</name>
  <files>src/lib/scoring/league.ts, src/lib/scoring/props.ts</files>
  <read_first>
    - .planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-RESEARCH.md (Pattern 4 lines 488-552 — full code skeletons for both functions, verbatim copy target)
    - .planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-CONTEXT.md (D-17 — "src/lib/scoring/ as pure TypeScript"; D-19 — kind enum values)
    - .planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-PATTERNS.md (No-Analog-Found section — both files are net-new)
    - .planning/REQUIREMENTS.md (SCR-01 — 4/3/2/0 scoring; SCR-02 — 90-min only; SCR-04 — admin-set point value; SCR-05 — integer-only)
    - src/lib/schemas/displayName.ts (for `.normalize('NFC').toLowerCase()` normalization pattern, copied by scoreProp)
  </read_first>
  <behavior>
    `scoreMatch(p, r)` returns:
    - `{ points: 4, kind: 'exact' }` when `p.home_score === r.result_home_90min && p.away_score === r.result_away_90min`
    - `{ points: 3, kind: 'goal-diff' }` when `(p.home_score - p.away_score) === (r.result_home_90min - r.result_away_90min) && diff !== 0` (excludes 0-0 vs 1-1 since 0 == 0 would be a different category)
    - `{ points: 2, kind: 'winner' }` when `Math.sign(p.home_score - p.away_score) === Math.sign(r.result_home_90min - r.result_away_90min)` (covers both-positive, both-negative, both-zero-but-not-exact)
    - `{ points: 0, kind: 'miss' }` otherwise

    `scoreProp({ userAnswer, correctAnswer, answerType, pointsAtStake, aliases })` returns:
    - `{ points: pointsAtStake, kind: 'correct' }` when `userAnswer.trim().normalize('NFC').toLowerCase()` is in the Set built from `[correctAnswer, ...aliases].map(s => s.trim().normalize('NFC').toLowerCase())`
    - `{ points: 0, kind: 'miss' }` otherwise

    Property tests / cases to validate manually after authoring (note: this phase does NOT install Vitest per CLAUDE.md — write a 10-line node script under `scripts/scoring-smoke.ts` that imports the two functions and asserts the cases below, then run it via `tsx`):
    - `scoreMatch({home:2,away:1}, {result_home_90min:2,result_away_90min:1})` = `{points:4,kind:'exact'}`
    - `scoreMatch({home:2,away:1}, {result_home_90min:3,result_away_90min:2})` = `{points:3,kind:'goal-diff'}` (both +1)
    - `scoreMatch({home:1,away:1}, {result_home_90min:2,result_away_90min:2})` = `{points:2,kind:'winner'}` (both 0-diff; "draw winner")
    - `scoreMatch({home:2,away:1}, {result_home_90min:1,result_away_90min:0})` = `{points:2,kind:'winner'}` (both +diff; not exact; different diff)
    - `scoreMatch({home:0,away:0}, {result_home_90min:1,result_away_90min:0})` = `{points:0,kind:'miss'}` (predicted draw, real away win)
    - `scoreMatch({home:2,away:0}, {result_home_90min:0,result_away_90min:2})` = `{points:0,kind:'miss'}` (opposite winner)
    - `scoreProp({userAnswer:'Messi',correctAnswer:'Lionel Messi',answerType:'single-player',pointsAtStake:5,aliases:['Messi','L. Messi']})` = `{points:5,kind:'correct'}`
    - `scoreProp({userAnswer:'  MESSI  ',correctAnswer:'Lionel Messi',answerType:'single-player',pointsAtStake:5,aliases:['Messi']})` = `{points:5,kind:'correct'}` (trim+lower)
    - `scoreProp({userAnswer:'Ronaldo',correctAnswer:'Lionel Messi',answerType:'single-player',pointsAtStake:5,aliases:[]})` = `{points:0,kind:'miss'}`
    - `scoreProp({userAnswer:'<team-uuid>',correctAnswer:'<team-uuid>',answerType:'single-team',pointsAtStake:3,aliases:[]})` = `{points:3,kind:'correct'}`
  </behavior>
  <action>
    Create `src/lib/scoring/league.ts` with the exact shape from RESEARCH Pattern 4 (lines 494-516). Verbatim, including the `Math.sign(...)===Math.sign(...)` check for `winner`. Public exports: `scoreMatch`, types `LeagueKind`, `LeagueScore`, `LeaguePrediction`, `LeagueResult`.

    ```typescript
    // src/lib/scoring/league.ts

    export type LeagueKind = 'exact' | 'goal-diff' | 'winner' | 'miss';
    export type LeagueScore = { points: number; kind: LeagueKind };

    export type LeaguePrediction = { home_score: number; away_score: number };
    export type LeagueResult = { result_home_90min: number; result_away_90min: number };

    /**
     * SCR-01: 4 (exact) / 3 (correct non-zero goal-diff) / 2 (correct winner or 0-diff non-exact) / 0
     * SCR-02: only the 90-min columns matter; ET/penalty columns ignored
     * SCR-05: integer-only — all inputs smallint, output `points: number` is integer-shaped
     */
    export function scoreMatch(p: LeaguePrediction, r: LeagueResult): LeagueScore {
      if (p.home_score === r.result_home_90min && p.away_score === r.result_away_90min) {
        return { points: 4, kind: 'exact' };
      }
      const predDiff = p.home_score - p.away_score;
      const realDiff = r.result_home_90min - r.result_away_90min;
      if (predDiff === realDiff && predDiff !== 0) {
        return { points: 3, kind: 'goal-diff' };
      }
      if (Math.sign(predDiff) === Math.sign(realDiff)) {
        return { points: 2, kind: 'winner' };
      }
      return { points: 0, kind: 'miss' };
    }
    ```

    Create `src/lib/scoring/props.ts` from RESEARCH Pattern 4 lines 518-547. Public exports: `scoreProp`, types `PropAnswerType`, `PropScore`. Important: `answer_type` values match the DB CHECK constraint from 0001_init.sql (`'single-team' | 'single-player' | 'text'` — verify against the seeded `prop_questions` rows by reading 0001_init.sql:144 if uncertain about the dashes vs underscores).

    ```typescript
    // src/lib/scoring/props.ts

    export type PropAnswerType = 'single-team' | 'single-player' | 'text';
    export type PropScore = { points: number; kind: 'correct' | 'miss' };

    /**
     * SCR-04: per-question admin-set point value (`pointsAtStake`).
     * D-24: trim + NFC + lowercase, then membership-check against {correctAnswer ∪ aliases}.
     * For 'single-team', userAnswer is a UUID — exact match (normalization is a no-op).
     * For 'single-player' / 'text', alias set drives fuzzy match (D-24).
     */
    export function scoreProp(opts: {
      userAnswer: string;
      correctAnswer: string;
      answerType: PropAnswerType;
      pointsAtStake: number;
      aliases?: readonly string[];
    }): PropScore {
      const norm = (s: string) => s.trim().normalize('NFC').toLowerCase();
      const target = norm(opts.correctAnswer);
      const aliasSet = new Set([target, ...(opts.aliases ?? []).map(norm)]);

      const isCorrect = aliasSet.has(norm(opts.userAnswer));
      return isCorrect
        ? { points: opts.pointsAtStake, kind: 'correct' }
        : { points: 0, kind: 'miss' };
    }
    ```

    **Smoke test:** Create `scripts/scoring-smoke.ts`:

    ```typescript
    // scripts/scoring-smoke.ts — non-test sanity check; run via `tsx scripts/scoring-smoke.ts`
    import { scoreMatch } from '../src/lib/scoring/league';
    import { scoreProp } from '../src/lib/scoring/props';

    const assert = (label: string, cond: boolean) => {
      if (!cond) { console.error(`FAIL: ${label}`); process.exit(1); }
      console.log(`ok: ${label}`);
    };

    // SCR-01 cases
    assert('exact 2-1 vs 2-1', JSON.stringify(scoreMatch({home_score:2,away_score:1},{result_home_90min:2,result_away_90min:1})) === '{"points":4,"kind":"exact"}');
    assert('diff 2-1 vs 3-2', JSON.stringify(scoreMatch({home_score:2,away_score:1},{result_home_90min:3,result_away_90min:2})) === '{"points":3,"kind":"goal-diff"}');
    assert('draw 1-1 vs 2-2', JSON.stringify(scoreMatch({home_score:1,away_score:1},{result_home_90min:2,result_away_90min:2})) === '{"points":2,"kind":"winner"}');
    assert('winner 2-1 vs 1-0', JSON.stringify(scoreMatch({home_score:2,away_score:1},{result_home_90min:1,result_away_90min:0})) === '{"points":2,"kind":"winner"}');
    assert('miss draw vs away-win', JSON.stringify(scoreMatch({home_score:0,away_score:0},{result_home_90min:1,result_away_90min:0})) === '{"points":0,"kind":"miss"}');
    assert('miss opposite winner', JSON.stringify(scoreMatch({home_score:2,away_score:0},{result_home_90min:0,result_away_90min:2})) === '{"points":0,"kind":"miss"}');

    // SCR-04 cases
    assert('prop correct alias', scoreProp({userAnswer:'Messi',correctAnswer:'Lionel Messi',answerType:'single-player',pointsAtStake:5,aliases:['Messi','L. Messi']}).points === 5);
    assert('prop correct trim+lower', scoreProp({userAnswer:'  MESSI  ',correctAnswer:'Lionel Messi',answerType:'single-player',pointsAtStake:5,aliases:['Messi']}).points === 5);
    assert('prop miss', scoreProp({userAnswer:'Ronaldo',correctAnswer:'Lionel Messi',answerType:'single-player',pointsAtStake:5,aliases:[]}).points === 0);
    assert('prop team uuid match', scoreProp({userAnswer:'abc-uuid',correctAnswer:'abc-uuid',answerType:'single-team',pointsAtStake:3,aliases:[]}).points === 3);

    console.log('all scoring smokes pass');
    ```

    Run `npx tsx scripts/scoring-smoke.ts` — must print all `ok:` lines and exit 0.

    **No `'server-only'` directive on these files** — pure scoring may be imported anywhere (including a future client-side preview UI). Server-only directive only applies to modules that touch service-role or DB.
  </action>
  <verify>
    <automated>npx tsx scripts/scoring-smoke.ts && grep -c "export function scoreMatch" src/lib/scoring/league.ts && grep -c "export function scoreProp" src/lib/scoring/props.ts && grep -c "Math.sign(predDiff) === Math.sign(realDiff)" src/lib/scoring/league.ts && grep -c "normalize('NFC').toLowerCase" src/lib/scoring/props.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/scoring/league.ts` contains exact tokens `export function scoreMatch`, `export type LeagueKind = 'exact' | 'goal-diff' | 'winner' | 'miss'`, `Math.sign(predDiff) === Math.sign(realDiff)`, the `predDiff !== 0` exclusion for `goal-diff`, and a `return { points: 4, kind: 'exact' }` branch
    - `src/lib/scoring/props.ts` contains exact tokens `export function scoreProp`, `export type PropAnswerType = 'single-team' | 'single-player' | 'text'`, a `normalize('NFC').toLowerCase()` chain, a `Set` of aliases, and a `pointsAtStake` field on the input
    - Neither file imports from `@/lib/supabase/*` or `@supabase/*` (pure functions, no DB)
    - `npx tsx scripts/scoring-smoke.ts` exits 0 and prints "all scoring smokes pass" — all 10 assertions hold
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Pure scoring functions exist, behave per SCR-01/02/04, and pass the smoke script.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author sweepAndUpsert.ts + adminReadClient.ts (server-only helpers)</name>
  <files>src/lib/scoring/sweepAndUpsert.ts, src/lib/auth/adminReadClient.ts</files>
  <read_first>
    - .planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-PATTERNS.md (lines 340-396 for sweepAndUpsert skeleton + lines 310-340 for adminReadClient)
    - .planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-RESEARCH.md (Pattern 5 lines 554-612 for sweep semantics; Pitfall 6 lines 803-811 for per-locale revalidatePath; Pitfall 10 lines 838-843 for adminReadClient rationale)
    - src/lib/auth/session.ts (existing requireAdmin shape)
    - src/lib/supabase/service.ts (existing createServiceClient shape + the `'server-only'` directive convention)
    - src/types/supabase.ts (look for `Database['public']['Tables']['score_events']['Insert']` to type the rows correctly)
  </read_first>
  <action>
    Create `src/lib/scoring/sweepAndUpsert.ts` with `'server-only'` directive at top (PATTERNS Pattern D). Function signature exactly as PATTERNS lines 354-392:

    ```typescript
    // src/lib/scoring/sweepAndUpsert.ts
    import 'server-only';
    import type { SupabaseClient } from '@supabase/supabase-js';
    import { revalidatePath } from 'next/cache';
    import type { Database } from '@/types/supabase';

    type ScoreEventRow = {
      user_id: string;
      source: 'league' | 'prop' | 'bracket';
      ref_id: string;
      points: number;
      kind: 'exact' | 'goal-diff' | 'winner' | 'miss' | 'correct' | null;
    };

    /**
     * D-18 + RESEARCH Pattern 5: idempotent points projection.
     * Called by saveResult (source='league') and gradeProp (source='prop').
     *
     * 1. DELETE any score_events rows for this (source, ref_id) where the user_id
     *    is NOT in the new rows set — handles "user deleted their prediction" edge.
     * 2. Bulk UPSERT on the PK (user_id, source, ref_id) — idempotent by construction.
     * 3. revalidatePath() for 8 explicit per-locale × per-page combos (Pitfall 6).
     *
     * Returns { ok: true } on success, { ok: false, error: <message> } on DB failure.
     */
    export async function sweepAndUpsert(opts: {
      svc: SupabaseClient<Database>;
      source: 'league' | 'prop';
      ref_id: string;
      rows: ReadonlyArray<ScoreEventRow>;
    }): Promise<{ ok: true } | { ok: false; error: string }> {
      const { svc, source, ref_id, rows } = opts;

      // 1. Sweep stragglers (RESEARCH Pattern 5 edge case lines 610-611).
      const keepUserIds = rows.map((r) => r.user_id);
      // PostgREST `.not('user_id','in',...)` syntax requires comma-joined IDs in parens.
      // If the set is empty, pass '(null)' so the NOT IN doesn't match anyone (no-op delete).
      const inList = keepUserIds.length > 0 ? `(${keepUserIds.join(',')})` : '(null)';
      const { error: delErr } = await svc
        .from('score_events')
        .delete()
        .eq('source', source)
        .eq('ref_id', ref_id)
        .not('user_id', 'in', inList);
      if (delErr) return { ok: false, error: `sweep:${delErr.message}` };

      // 2. Bulk UPSERT (D-18 idempotency via PK).
      if (rows.length > 0) {
        const { error: upErr } = await svc
          .from('score_events')
          .upsert(rows as ScoreEventRow[], { onConflict: 'user_id,source,ref_id' });
        if (upErr) return { ok: false, error: `upsert:${upErr.message}` };
      }

      // 3. Revalidate per-locale per-page (Pitfall 6). Eight explicit paths.
      for (const path of [
        '/he/leaderboard', '/en/leaderboard',
        '/he/matches',     '/en/matches',
        '/he/me',          '/en/me',
        '/he/props',       '/en/props',
      ]) {
        revalidatePath(path);
      }

      return { ok: true };
    }
    ```

    Create `src/lib/auth/adminReadClient.ts` (PATTERNS lines 310-340 + RESEARCH Open Q3):

    ```typescript
    // src/lib/auth/adminReadClient.ts
    import 'server-only';
    import { requireAdmin } from '@/lib/auth/session';
    import { createServiceClient } from '@/lib/supabase/service';

    /**
     * Pitfall 10: admin RSCs using createClient() (anon JWT) hit RLS and see
     * only the admin's own predictions. This helper gates on requireAdmin()
     * and returns a service-role client that BYPASSES RLS — required for the
     * integrity widget (reads all predictions), the admin matches list
     * (shows everyone's picks for entry mode), the roster (lists all profiles),
     * and the tournament-tree (resolves placeholders across fixtures).
     *
     * IMPORTANT: requireAdmin() may redirect() — do NOT wrap the call in a
     * try/catch that swallows redirects. Let Next.js handle the redirect throw.
     */
    export async function adminReadClient() {
      await requireAdmin();
      return createServiceClient();
    }
    ```

    Both files MUST begin with `import 'server-only';` (PATTERNS Pattern D — fails build if pulled into client bundle).
  </action>
  <verify>
    <automated>grep -c "import 'server-only'" src/lib/scoring/sweepAndUpsert.ts && grep -c "import 'server-only'" src/lib/auth/adminReadClient.ts && grep -c "export async function sweepAndUpsert" src/lib/scoring/sweepAndUpsert.ts && grep -c "export async function adminReadClient" src/lib/auth/adminReadClient.ts && grep -c "onConflict: 'user_id,source,ref_id'" src/lib/scoring/sweepAndUpsert.ts && grep -c "/he/leaderboard" src/lib/scoring/sweepAndUpsert.ts && grep -c "/en/leaderboard" src/lib/scoring/sweepAndUpsert.ts && grep -c "await requireAdmin" src/lib/auth/adminReadClient.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/scoring/sweepAndUpsert.ts` first executable line is `import 'server-only';`
    - File contains tokens `export async function sweepAndUpsert`, `onConflict: 'user_id,source,ref_id'`, `.not('user_id', 'in'`, `revalidatePath`
    - File revalidates exactly 8 explicit paths: `/he/leaderboard`, `/en/leaderboard`, `/he/matches`, `/en/matches`, `/he/me`, `/en/me`, `/he/props`, `/en/props` (verify with `grep -c "revalidatePath('/" src/lib/scoring/sweepAndUpsert.ts` returns 8)
    - File handles the empty-rows case via `inList = '(null)'` fallback
    - `src/lib/auth/adminReadClient.ts` first executable line is `import 'server-only';`
    - File contains tokens `export async function adminReadClient`, `await requireAdmin()`, `return createServiceClient()`
    - `npm run typecheck` exits 0 (types from regenerated Plan 02-01 supabase.ts resolve)
  </acceptance_criteria>
  <done>Two server-only helpers exist with correct signatures and exact-string invariants; they import from the regenerated Database types and typecheck cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author Zod schemas (prediction, result, propAnswer, propAuthoring)</name>
  <files>src/lib/schemas/prediction.ts, src/lib/schemas/result.ts, src/lib/schemas/propAnswer.ts, src/lib/schemas/propAuthoring.ts</files>
  <read_first>
    - src/lib/schemas/displayName.ts (Phase 1 Zod pattern — `.trim()`, `.regex()`, error codes, type inference via `z.infer<typeof ...>`)
    - src/lib/schemas/join.ts (existing schema importing displayName for composition)
    - .planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-CONTEXT.md (D-04 — score 0-9 range; D-13 — prop authoring fields; D-24 — answer_type values; D-25 — answer_type per question)
    - .planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-PATTERNS.md (lines 398-422 for prediction schema shape; line 43-44 for propAnswer discriminated union note)
    - .planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-RESEARCH.md (Security Domain table lines 1112-1125 — XSS regex on free-text prop_answers.answer)
    - supabase/migrations/0001_init.sql (prop_questions definition — verify answer_type enum values exactly: `'single-team'`, `'single-player'`, `'text'`)
  </read_first>
  <action>
    Create four Zod-v4 schemas mirroring the displayName.ts shape.

    **`src/lib/schemas/prediction.ts`** (PATTERNS lines 405-415, per D-04 0-9 range):

    ```typescript
    // src/lib/schemas/prediction.ts
    import { z } from 'zod';

    /**
     * D-04: score range 0-9 each side. Coerced from FormData string.
     * Error codes mirror displayName.ts pattern — client maps to next-intl keys.
     */
    export const predictionSchema = z.object({
      fixture_id: z.string().uuid('fixture_id_invalid'),
      home_score: z.coerce.number().int('score_int').min(0, 'score_range').max(9, 'score_range'),
      away_score: z.coerce.number().int('score_int').min(0, 'score_range').max(9, 'score_range'),
    });

    export type PredictionInput = z.infer<typeof predictionSchema>;
    ```

    **`src/lib/schemas/result.ts`** (admin result entry; mirror prediction but for admin):

    ```typescript
    // src/lib/schemas/result.ts
    import { z } from 'zod';

    /**
     * Admin result entry per ADM-01 + D-12 (Phase 2 only populates _90min;
     * _full columns ship in 0009 but stay null until Phase 3 ET UI).
     * Range 0-9 matches the player's prediction range for symmetry.
     */
    export const resultSchema = z.object({
      fixture_id: z.string().uuid('fixture_id_invalid'),
      result_home_90min: z.coerce.number().int('score_int').min(0, 'score_range').max(9, 'score_range'),
      result_away_90min: z.coerce.number().int('score_int').min(0, 'score_range').max(9, 'score_range'),
    });

    export type ResultInput = z.infer<typeof resultSchema>;
    ```

    **`src/lib/schemas/propAnswer.ts`** (discriminated union by `answer_type` per D-22/D-23/D-24 + Security row "Stored XSS via prop_answers.answer"):

    ```typescript
    // src/lib/schemas/propAnswer.ts
    import { z } from 'zod';

    /**
     * D-23: single-team picker stores team_id UUID.
     * D-24: single-player and text use a free-text input; XSS regex rejects
     *   HTML/script chars per RESEARCH Security Domain table.
     *
     * Discriminated union by `answer_type` so the server action validates the
     * answer shape against the question's declared type.
     */
    const SINGLE_TEAM = z.object({
      question_id: z.string().uuid('question_id_invalid'),
      answer_type: z.literal('single-team'),
      answer: z.string().uuid('team_id_invalid'),
    });

    const FREE_TEXT_REGEX = /^[\p{L}\d \-.,!?']+$/u; // letters, digits, common punctuation; rejects <, >, &, /, etc.

    const SINGLE_PLAYER = z.object({
      question_id: z.string().uuid('question_id_invalid'),
      answer_type: z.literal('single-player'),
      answer: z.string().trim().min(1, 'answer_empty').max(64, 'answer_long').regex(FREE_TEXT_REGEX, 'answer_chars'),
    });

    const TEXT = z.object({
      question_id: z.string().uuid('question_id_invalid'),
      answer_type: z.literal('text'),
      answer: z.string().trim().min(1, 'answer_empty').max(120, 'answer_long').regex(FREE_TEXT_REGEX, 'answer_chars'),
    });

    export const propAnswerSchema = z.discriminatedUnion('answer_type', [SINGLE_TEAM, SINGLE_PLAYER, TEXT]);
    export type PropAnswerInput = z.infer<typeof propAnswerSchema>;
    ```

    **`src/lib/schemas/propAuthoring.ts`** (admin author + grade — per D-13 + D-24):

    ```typescript
    // src/lib/schemas/propAuthoring.ts
    import { z } from 'zod';

    /**
     * ADM-04 + D-13: admin authors a prop with bilingual prompts + answer_type + point value.
     * Admin pages are EN-only (Phase 1 D-05) but the prompts themselves are bilingual
     * because they render to players in both locales.
     */
    export const propAuthoringSchema = z.object({
      id: z.string().uuid().optional(),  // omitted on create; present on edit
      prompt_en: z.string().trim().min(3, 'prompt_short').max(200, 'prompt_long'),
      prompt_he: z.string().trim().min(3, 'prompt_short').max(200, 'prompt_long'),
      answer_type: z.enum(['single-team', 'single-player', 'text']),
      points_value: z.coerce.number().int().min(1, 'points_range').max(50, 'points_range'),
    });
    export type PropAuthoringInput = z.infer<typeof propAuthoringSchema>;

    /**
     * D-13 + D-24: grading takes the canonical correct answer + an alias set
     * (newline-separated in the UI textarea, normalized to text[] server-side).
     */
    export const propGradingSchema = z.object({
      id: z.string().uuid('question_id_invalid'),
      correct_answer: z.string().trim().min(1, 'answer_empty').max(120, 'answer_long'),
      correct_answer_aliases: z.array(z.string().trim().min(1)).max(20, 'too_many_aliases').default([]),
    });
    export type PropGradingInput = z.infer<typeof propGradingSchema>;
    ```

    All four files MUST follow the displayName.ts pattern: `import { z } from 'zod';` at top, code-only error tokens (no full English error strings — client maps via next-intl), exported type via `z.infer<typeof ...>`.

    Append a tiny additional smoke to `scripts/scoring-smoke.ts` (or create `scripts/schemas-smoke.ts`) that imports each schema and `parse`s one valid + one invalid input. Run via `npx tsx`.
  </action>
  <verify>
    <automated>grep -c "export const predictionSchema" src/lib/schemas/prediction.ts && grep -c "export const resultSchema" src/lib/schemas/result.ts && grep -c "discriminatedUnion" src/lib/schemas/propAnswer.ts && grep -c "export const propAuthoringSchema" src/lib/schemas/propAuthoring.ts && grep -c "export const propGradingSchema" src/lib/schemas/propAuthoring.ts && grep -c ".max(9, 'score_range')" src/lib/schemas/prediction.ts && grep -c ".max(9, 'score_range')" src/lib/schemas/result.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/schemas/prediction.ts` contains tokens `export const predictionSchema`, `z.string().uuid('fixture_id_invalid')`, `.min(0, 'score_range')`, `.max(9, 'score_range')`, `.coerce.number()`, `export type PredictionInput = z.infer<typeof predictionSchema>`
    - `src/lib/schemas/result.ts` contains tokens `export const resultSchema`, `result_home_90min`, `result_away_90min`, same 0-9 range pattern, `export type ResultInput`
    - `src/lib/schemas/propAnswer.ts` contains tokens `discriminatedUnion`, `z.literal('single-team')`, `z.literal('single-player')`, `z.literal('text')`, the `FREE_TEXT_REGEX` constant with `/^[\p{L}\d \-.,!?']+$/u`, and `export const propAnswerSchema`
    - `src/lib/schemas/propAuthoring.ts` contains tokens `export const propAuthoringSchema`, `prompt_en`, `prompt_he`, `z.enum(['single-team', 'single-player', 'text'])`, `export const propGradingSchema`, `correct_answer_aliases: z.array(z.string()`
    - `npm run typecheck` exits 0
    - Optional smoke (if `scripts/schemas-smoke.ts` is added): `npx tsx scripts/schemas-smoke.ts` exits 0 with all parses producing the expected `success: true/false` outcome
  </acceptance_criteria>
  <done>Four Zod schemas exist, mirror Phase 1 patterns, encode all CONTEXT.md range/enum constraints, and typecheck cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Commit Plan 02-02 changes (single commit)</name>
  <files>src/lib/scoring/league.ts, src/lib/scoring/props.ts, src/lib/scoring/sweepAndUpsert.ts, src/lib/auth/adminReadClient.ts, src/lib/schemas/prediction.ts, src/lib/schemas/result.ts, src/lib/schemas/propAnswer.ts, src/lib/schemas/propAuthoring.ts, scripts/scoring-smoke.ts</files>
  <read_first>
    - .planning/STATE.md (git author rewrite note)
  </read_first>
  <action>
    1. Verify git author: `git config user.email`. If not `10100761+zarurc@users.noreply.github.com`, set it: `git config user.email '10100761+zarurc@users.noreply.github.com'`.
    2. Run all lints + typecheck + smoke: `npm run lint:rtl && npm run lint:tailwind-v4 && npm run typecheck && npx tsx scripts/scoring-smoke.ts`. All must exit 0.
    3. Stage and commit: `git add src/lib/scoring/league.ts src/lib/scoring/props.ts src/lib/scoring/sweepAndUpsert.ts src/lib/auth/adminReadClient.ts src/lib/schemas/prediction.ts src/lib/schemas/result.ts src/lib/schemas/propAnswer.ts src/lib/schemas/propAuthoring.ts scripts/scoring-smoke.ts && git commit -m 'feat(02-02): scoring engine + sweep helper + adminReadClient + Zod schemas'`.
    4. Verify clean tree: `git status` produces no `M ` or `??` lines.
  </action>
  <verify>
    <automated>git config user.email | grep -c "10100761+zarurc@users.noreply.github.com" && git status --porcelain | wc -l | tr -d ' ' | grep -c "^0$" && npm run lint:rtl && npm run lint:tailwind-v4 && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `git config user.email` returns `10100761+zarurc@users.noreply.github.com`
    - `git status --porcelain` produces zero lines
    - Top commit (`git log --oneline -1`) is the Plan 02-02 feat commit with all 8+1 files
    - All three lints + typecheck pass on the committed tree
  </acceptance_criteria>
  <done>Plan 02-02 changes are committed to the noreply email; tree is clean; all gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pure scoring lib → any caller | No DB, no IO, no secrets — safe to import anywhere |
| sweepAndUpsert → score_events (service-role) | Server-only; bypasses RLS; gated by caller's `requireAdmin()` |
| adminReadClient → all admin RSC reads | Server-only; gated by `requireAdmin()` (may redirect non-admin) |
| Zod schemas → Server Action input | Validation boundary — every Server Action that accepts external input MUST safeParse first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | `src/lib/supabase/service.ts` SECRET key leaked via accidental import in client bundle | mitigate | Both `sweepAndUpsert.ts` and `adminReadClient.ts` open with `import 'server-only';` (PATTERNS Pattern D). Build fails loudly if pulled into client. Verified by acceptance: grep on `'server-only'` at line 1. |
| T-02-02-02 | Tampering | A client-side caller bypasses Zod and posts arbitrary score (e.g., `home_score=999`) to the prediction Server Action | mitigate | Server Action ALWAYS calls `predictionSchema.safeParse(input)` before any DB call. Schema enforces `.min(0).max(9).int()`. RLS is the second gate (kickoff lock); Zod is the first (input shape). Verified once Plan 02-03 wires the action. |
| T-02-02-03 | Tampering | A client-side caller posts a fixture_id that doesn't exist OR isn't a UUID | mitigate | `predictionSchema.fixture_id = z.string().uuid()`. Postgres FK on `predictions.fixture_id` rejects unknown IDs (`23503`). |
| T-02-02-04 | Tampering | Admin grade-flow caller posts an alias array of 10,000 entries to exhaust regex/normalization | mitigate | `propGradingSchema.correct_answer_aliases.max(20)` bounds it. |
| T-02-02-05 | XSS (Tampering/Info Disclosure) | User posts `<script>alert(1)</script>` as their `prop_answer.answer` (free text) | mitigate | `propAnswerSchema` SINGLE_PLAYER + TEXT branches use `FREE_TEXT_REGEX = /^[\p{L}\d \-.,!?']+$/u` — rejects `<`, `>`, `&`, `/`, `=`, etc. React auto-escapes on render as second defense. |
| T-02-02-06 | EoP | `adminReadClient` called from a non-admin RSC by accident returns service-role client | mitigate | `await requireAdmin()` first — redirects non-admin to `/admin/403` per Phase 1. Service-role client only returned to confirmed admin. |
| T-02-02-07 | Tampering | Idempotency-bypass: admin saves result, score_events get written, then a concurrent direct REST INSERT on `score_events` (via leaked service key) double-counts | mitigate | PK `(user_id, source, ref_id)` is the database-level idempotency key. Any duplicate insert with same key triggers `ON CONFLICT DO UPDATE` — points overwrite, not append. Even with key leak, the data shape stays correct. |
</threat_model>

<verification>
- All 8 source files exist with correct exports (verified by grep gates in each task).
- `npx tsx scripts/scoring-smoke.ts` exits 0 — all 10 scoring assertions hold.
- `npm run typecheck` exits 0 — types reference `src/types/supabase.ts` from Plan 02-01.
- `npm run lint:tailwind-v4` exits 0 — pure TS files don't introduce `[--zc-X]` regressions.
- All files are committed to git authored to the noreply email.
</verification>

<success_criteria>
- `scoreMatch` and `scoreProp` exist as pure functions returning `{ points, kind }`; smoke script proves all SCR-01/02/04 cases.
- `sweepAndUpsert` is a server-only helper that DELETEs stragglers then UPSERTs on `(user_id, source, ref_id)` and revalidates 8 explicit per-locale paths.
- `adminReadClient` composes `requireAdmin()` + `createServiceClient()` for use by every admin RSC.
- Four Zod schemas (prediction, result, propAnswer, propAuthoring) exist with discriminated unions / coercion / regex sanitization where applicable.
- Tree is clean; all lints + typecheck pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-02-SUMMARY.md` documenting:
- 8+1 file inventory (paths, purpose, exports)
- The 10 scoring smoke assertions and their pass output
- Confirmation of `'server-only'` directive on sweepAndUpsert + adminReadClient
- Any deviations from the plan (per Phase 1 Rule 1/2/3 deviation taxonomy)
</output>
