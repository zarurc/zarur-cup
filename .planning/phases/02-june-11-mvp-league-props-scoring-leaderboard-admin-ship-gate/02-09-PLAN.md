---
phase: 02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate
plan: 09
type: execute
wave: 7
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PROJECT-UPD-01
  - PROJECT-UPD-02
  - PROJECT-UPD-03

tags: [docs, roadmap, scope-expansion]

must_haves:
  truths:
    - "PROJECT.md 'Out of Scope' no longer contains 'Live external sports API integration'"
    - "PROJECT.md 'Out of Scope' contains 'Bracket Mode as a prediction game' with displacement reason"
    - "PROJECT.md 'Active Requirements' replaces 'Bracket Mode' line with 'Read-only bracket view'"
    - "PROJECT.md 'Active Requirements' replaces 'Bracket scoring escalates per round' line (removed; no scoring for read-only view)"
    - "PROJECT.md 'Key Decisions' table marks 'Bracket scoring escalates' as cut and 'Admin enters fixtures + results manually' as partial (auto-fetch added)"
    - "ROADMAP.md no longer plans Phase 3 'Bracket Mode (Pre-Knockout Ship)' as a future phase — either removed or renamed to a post-launch polish slot"
    - "ROADMAP.md Phase 2 plan list includes 02-09 through 02-12 with checkbox state matching shipped/pending"
    - "ROADMAP.md 'Out-of-Scope' section reflects the same reversal/cut deltas as PROJECT.md"
    - "REQUIREMENTS.md adds the four new requirement ID families (AUTO-*, BRK-VIEW-*, PRIVATE-*, PROJECT-UPD-*) under a 'Scope Expansion' subsection with traceability entries; existing BRK-01..04, VIS-03, SCR-03 marked as cut"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Updated scope statement — auto-fetch in, bracket prediction out"
      contains: "Bracket Mode as a prediction game"
    - path: ".planning/ROADMAP.md"
      provides: "Updated phase plan — Phase 3 cancelled, Phase 2 expanded"
      contains: "02-09-PLAN.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Scope expansion requirement IDs"
      contains: "AUTO-01"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/ROADMAP.md"
      via: "consistent scope statement"
      pattern: "Read-only bracket view"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "traceability table — each scope-expansion REQ ID maps to a Phase 2 plan"
      pattern: "AUTO-01.*Phase 2"
---

<objective>
Update three planning source-of-truth documents to reflect the 2026-05-26 scope expansion: (a) Bracket-as-prediction-game is cut (D-34), (b) external sports API integration is reversed back into scope (D-36), (c) Props are strictly private (D-38), (d) read-only bracket view replaces the EmptyStateCard placeholder (D-40/D-47). All three docs (PROJECT.md, ROADMAP.md, REQUIREMENTS.md) must be self-consistent at the end of this plan — they are the canonical reference for every subsequent agent and human auditor.

Purpose: Without this, future planners/researchers/executors will keep reading stale OOS lines + Phase-3 placeholders and produce work against an outdated vision. Doc-only work, zero code, low risk.

Output: Three .md files updated and committed; no Supabase, Vercel, or runtime touch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-CONTEXT.md
@.planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-RESEARCH-ADDENDUM.md
@.planning/STATE.md

<interfaces>
<!-- No code interfaces — this plan is doc-only. -->
<!-- Reference: PROJECT.md "Out of Scope" section (lines 38-50 at time of plan writing), -->
<!-- "Active Requirements" (lines 23-36), "Key Decisions" table (lines 77-87). -->
<!-- ROADMAP.md "Phases" (lines 28-30), "Out-of-Scope" (lines 169-179), Phase 2 plan list (lines 92-99). -->
<!-- REQUIREMENTS.md traceability table (line 200+ region). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PROJECT.md — reverse OOS on external API, cut bracket prediction game, mark key decisions</name>
  <read_first>
    - .planning/PROJECT.md (the entire file — read once, line-of-truth for "Active Requirements", "Out of Scope", "Key Decisions")
    - .planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-CONTEXT.md (specifically the `<scope_expansion_addendum>` block — D-34..D-47, especially D-42 which dictates this task's exact edits)
  </read_first>
  <action>
Make the following exact textual edits to `.planning/PROJECT.md`:

**Edit 1: "Active Requirements" section (around line 23-36)**

Find the line `- [ ] **Bracket Mode**: knockout bracket predictions (R16 → Final + Winner)` and REPLACE it with:
```
- [ ] **Bracket View** (read-only): server-rendered tree view of the knockout bracket that fills in live as admin enters results; no user predictions, no scoring
```

Find the line `- [ ] Bracket scoring (escalating per round) — R16 = 2, QF = 4, SF = 8, F = 16, Champion = 32` and REMOVE it entirely (no bracket scoring per D-34).

Find the line `- [ ] **Props/Wildcards**: tournament-level bonus questions (e.g. winner, top scorer) — locked before the tournament's first kickoff` and REPLACE it with:
```
- [ ] **Props/Wildcards** (strictly private): tournament-level bonus questions answered before first kickoff; **each user sees only their own answers at all times — pre-lock editable, post-lock read-only receipt — no cross-user reveal** (per D-38)
```

**Edit 2: "Out of Scope" section (around line 38-50)**

REMOVE the line `- **Live external sports API integration** — admin enters results manually; pre-seeded fixtures are sufficient for one tournament, less infra to break` (D-36 reverses this).

ADD a new bullet under "Out of Scope" (alphabetize within the existing list, or append at end — append for minimum diff churn):
```
- **Bracket Mode as a prediction game** — displaced by read-only bracket view in v1 (per Phase 2 D-34, 2026-05-26). The `bracket_picks` table remains in the schema as a harmless empty artifact; no UI surface writes to it.
- **Auto-grade props from public sources** — tournament-level awards (Top Scorer, Golden Boot, Golden Ball) are announced after the final on July 19, 2026; no live data source publishes them in a polling-friendly shape. Admin grades manually via `/admin/props` (~10 minutes for 7 props once the awards are announced) per Phase 2 D-35.
```

**Edit 3: "Key Decisions" table (around line 77-87)**

Find the row `| Bracket scoring escalates per round (2/4/8/16/32) | Rewards picking the right finalists/champion, not just early-round noise | — Pending |` and REPLACE the Outcome column from `— Pending` to `❌ Cut — Phase 2 D-34 (2026-05-26): displaced by read-only bracket view; no bracket prediction game ships`.

Find the row `| Admin enters fixtures + results manually (pre-seed the WC 2026 schedule once) | Avoids external sports-API dependency and rate limits; one trusted operator | — Pending |` and REPLACE the Outcome column from `— Pending` to `🔶 Partial — Phase 2 D-36 (2026-05-26): auto-fetch from football-data.org v4 added for in-tournament score ingestion; admin manual entry at /admin/matches remains the canonical fallback and override path`.

ADD a new row to the Key Decisions table (after the existing rows):
```
| Props strictly private — no cross-user reveal ever | Family-trust posture is mutual interest in own picks; rivalry visible only through leaderboard points totals | ✅ Locked — Phase 2 D-38 (2026-05-26) |
| External sports API integration permitted for match-score auto-fetch only | Removes 104×manual-entry chore over 30-day tournament; admin override always available | ✅ Locked — Phase 2 D-36 (2026-05-26): football-data.org v4 selected per D-46; Supabase pg_cron + pg_net (per D-45) bypasses Vercel Hobby's once-per-day cron cap |
| Read-only bracket view replaces Bracket Mode prediction game | Lower scope, immediate value (family sees tree fill during knockouts), zero scoring fragility | ✅ Locked — Phase 2 D-40 + D-47 (2026-05-26): column-of-rounds RSC at `/[locale]/bracket`; live-fills via revalidatePath from saveResult |
```

**Edit 4: Update "Last updated" footer**

Update `*Last updated: 2026-05-23 after initialization*` to `*Last updated: 2026-05-26 after Phase 2 scope expansion (D-34..D-47)*`.

**Constraints:**
- Preserve existing markdown formatting (headers, table pipes, list bullets).
- Do NOT touch the "Core Value" line — it remains accurate.
- Do NOT touch the Constraints section — timeline + budget + identity + locking are unchanged.
- Do NOT touch the auth implementation strategy row — Phase 1 settled it (signInAnonymously + invite gate) but the Decision Outcome stays as-is for the milestone-flip job (`/gsd-complete-milestone`), not this plan.
  </action>
  <verify>
    <automated>grep -c "Live external sports API integration" .planning/PROJECT.md | grep -v '^#' | tr -d '[:space:]' | grep -qx 0 &amp;&amp; grep -q "Bracket Mode as a prediction game" .planning/PROJECT.md &amp;&amp; grep -q "Auto-grade props from public sources" .planning/PROJECT.md &amp;&amp; grep -q "Read-only bracket view replaces Bracket Mode prediction game" .planning/PROJECT.md &amp;&amp; grep -q "❌ Cut — Phase 2 D-34" .planning/PROJECT.md &amp;&amp; grep -q "🔶 Partial — Phase 2 D-36" .planning/PROJECT.md &amp;&amp; grep -q "Bracket View" .planning/PROJECT.md &amp;&amp; grep -q "strictly private" .planning/PROJECT.md &amp;&amp; grep -q "2026-05-26 after Phase 2 scope expansion" .planning/PROJECT.md &amp;&amp; ! grep -q "Bracket scoring (escalating per round)" .planning/PROJECT.md &amp;&amp; echo "PROJECT.md edits verified"</automated>
  </verify>
  <done>PROJECT.md "Out of Scope" no longer has the external-API line; has the bracket-prediction-cut line and auto-grade-cut line. Key Decisions table reflects D-34 / D-36 / D-38 / D-40 outcomes. Active Requirements describes read-only bracket view + private props. Footer date is 2026-05-26.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md — cancel Phase 3, update Phase 2 plan list, reflect OOS deltas</name>
  <read_first>
    - .planning/ROADMAP.md (the entire file; the Phases list at line 28-30, Phase 3 Phase Details at line 103-122, Out-of-Scope at line 169-179, Progress Table at line 123-129)
    - .planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-CONTEXT.md (`<scope_expansion_addendum>` D-34 + D-42 — the directives for ROADMAP edits)
  </read_first>
  <action>
Make the following exact textual edits to `.planning/ROADMAP.md`:

**Edit 1: "Bracket Mode Scope Decision" section (around line 16-24)**

REPLACE the entire `## Bracket Mode Scope Decision` block with a new section titled `## Bracket Scope Decision (revised 2026-05-26)`. Body:
```
**Bracket Mode as a prediction game is cancelled.** Phase 3 originally planned per-slot R32/R16/QF/SF/F/Champion picks with escalating 2/4/8/16/32 scoring; this is dropped per Phase 2 D-34 (2026-05-26).

Replacement: a **read-only bracket view** at `/[locale]/bracket` (replacing the Phase 1 EmptyStateCard) — server-rendered column-of-rounds RSC that fills in live as admin enters knockout results. No user predictions, no bracket scoring, no `source='bracket'` rows in `score_events`. Implemented in Phase 2 (Plan 02-11) by the original June 11 deadline, not a separate Phase 3.

Rationale:
- Lower scope than a prediction game; ships within Phase 2's existing 16-day window.
- Family still gets the "tree fill in live" excitement during the knockouts without the engineering burden of bracket scoring + reveal granularity + per-slot pick UX.
- The `bracket_picks` schema (Phase 1 0001_init.sql) remains as a harmless empty table — append-only convention forbids dropping a pushed migration.
```

**Edit 2: "Phases" list (around line 28-30)**

REPLACE the existing list:
```
- [x] **Phase 1: Foundation, Schema, Auth & RLS** — Bilingual Next.js shell + Supabase schema + invite-code auth + RLS lock/reveal policies + WC 2026 data seed (COMPLETE 2026-05-24; live at https://zarur-cup.vercel.app)
- [ ] **Phase 2: June 11 MVP — League + Props + Scoring + Leaderboard + Admin + Ship Gate** — End-to-end predict-lock-score-reveal-leaderboard flow; props; admin result entry; Playwright smoke; family invite distributed
- [ ] **Phase 3: Bracket Mode (Pre-Knockout Ship)** — Slot-based knockout picks + bracket scoring rolled into unified leaderboard, locked at first knockout
```

WITH:
```
- [x] **Phase 1: Foundation, Schema, Auth & RLS** — Bilingual Next.js shell + Supabase schema + invite-code auth + RLS lock/reveal policies + WC 2026 data seed (COMPLETE 2026-05-24; live at https://zarur-cup.vercel.app)
- [ ] **Phase 2: June 11 MVP — League + Props + Scoring + Leaderboard + Admin + Ship Gate + Scope Expansion** — End-to-end predict-lock-score-reveal-leaderboard flow; private props nested under /me; admin result entry + auto-fetch via Supabase pg_cron; read-only bracket view; Playwright smoke; family invite distributed
- ~~**Phase 3: Bracket Mode (Pre-Knockout Ship)**~~ — **CANCELLED 2026-05-26** per Phase 2 D-34. Replaced by read-only bracket view in Phase 2 Plan 02-11.
```

**Edit 3: Phase 2 Plan list (around line 90-99)**

APPEND the new plans 02-09 through 02-12 to the existing `Plans: 8 plans across 7 waves` list. Update the count to `Plans: 12 plans across 9 waves` (Plans 02-09..02-12 run across Waves 7-9 — 02-09 (docs) + 02-10 (props/RLS) are parallel-safe in Wave 7; 02-11 (bracket view, touches saveResult + sweepAndUpsert + messages overlapping 02-10) sequences after as Wave 8; 02-12 (auto-fetch, touches saveResult + types overlapping 02-11) sequences after as Wave 9).

ADD these four bullet items after the existing `- [ ] 02-08-PLAN.md` line:
```
- [ ] 02-09-PLAN.md — Wave 7 (scope expansion): PROJECT.md + ROADMAP.md + REQUIREMENTS.md updates — cut Bracket prediction game, reverse OOS on external API, document scope-expansion REQ IDs (doc-only)
- [ ] 02-10-PLAN.md — Wave 7 (scope expansion): Props strictly private + relocate to /me/props — new migration 0013_prop_answers_private.sql tightens RLS, page moved to nested route, /[locale]/props redirects, Me page gains Props card
- [ ] 02-11-PLAN.md — Wave 8 (scope expansion): Read-only bracket view at /[locale]/bracket — column-of-rounds RSC replaces EmptyStateCard, saveResult Server Action gains bracket revalidatePath + resolved_team_id writeback
- [ ] 02-12-PLAN.md — Wave 9 (scope expansion): Auto-fetch match scores — migration 0012_pg_cron_score_fetch.sql + 0014_fixtures_auto_fetched_at.sql, /api/score-fetch POST route with Bearer auth, football-data.org v4 client, Wave-0 fixture-mapping unit test
```

**Edit 4: "Phase 3" Phase Details (around line 103-122)**

PRESERVE the original `### Phase 3: Bracket Mode (Pre-Knockout Ship)` block content but prefix the entire section with the following banner at line ~103 (before the `### Phase 3:` heading):
```
> **CANCELLED 2026-05-26 per Phase 2 D-34.** Bracket-as-prediction-game displaced by read-only bracket view in Phase 2 Plan 02-11. Original requirements BRK-01..04, VIS-03, SCR-03 are moved to Out-of-Scope (see "Out-of-Scope" section below). Section retained for historical reference; no implementation work targets this phase.
```

Keep the rest of the section intact (Goal / Depends on / Target deadline / Requirements / Success Criteria) for forensic record.

**Edit 5: "Progress Table" (around line 123-129)**

UPDATE the Phase 2 row from `0/8 | Not started | -` to `0/12 | In progress (scope expanded 2026-05-26; plans 02-01..02-07 shipped; 02-08 partial; 02-09..02-12 new) | -`.

UPDATE the Phase 3 row from `0/TBD | Not started | -` to `— | Cancelled 2026-05-26 | -`.

**Edit 6: "Out-of-Scope" section (around line 169-179)**

After the existing bullet list, ADD:
```
- **Bracket Mode (as a prediction game with R32..Champion picks and 2/4/8/16/32 scoring)** — cancelled 2026-05-26 per Phase 2 D-34; displaced by read-only bracket view in Phase 2 Plan 02-11. Originally Phase 3 requirements BRK-01..04, VIS-03, SCR-03.
- **Auto-grade props** — cancelled 2026-05-26 per Phase 2 D-35; admin grades 7 props manually after July 19 tournament-end awards announcement.
```

REMOVE the literal substring `external sports API,` from the existing terse-list bullet at line ~179 (`- Real money, OAuth, public signup, external sports API, multi-admin, ...`).

**Edit 7: Coverage audit footer (around line 130-143)**

UPDATE the "Coverage Audit" section bottom comment to reflect:
- Phase 3 requirements moved to OOS: subtract `BRK-01..04, VIS-03, SCR-03` (6 requirements) from "Phase 3" and add to "Out of Scope (moved)".
- Phase 2 gains 4 new requirement IDs that compose to ~12 individual ID slots (AUTO-01..N + BRK-VIEW-01..N + PRIVATE-01..N + PROJECT-UPD-01..N) — Task 3 of this plan creates them in REQUIREMENTS.md; reference the new family count without enumerating.

Specifically update the Coverage Audit table:
```
| Phase | Requirements Mapped |
|-------|---------------------|
| Phase 1 | 26 (FND × 6, I18N × 7, AUTH × 7, DATA × 5, VIS-06 × 1) |
| Phase 2 | 34 original + scope-expansion families (AUTO-*, BRK-VIEW-*, PRIVATE-*, PROJECT-UPD-*) — see REQUIREMENTS.md |
| Phase 3 (cancelled) | 6 originals (BRK-01..04, VIS-03, SCR-03) → moved to Out-of-Scope 2026-05-26 |
| **Total** | 60 active v1 requirements + scope-expansion families; 6 moved to OOS |
```

**Edit 8: Footer**

Append after the existing trailing `*Roadmap created: 2026-05-23 by gsd-roadmapper*` line:
```
*Phase 2 scope expansion + Phase 3 cancellation: 2026-05-26*
```

**Constraints:**
- Preserve all markdown formatting (header levels, table pipes, list bullets, blockquote `>`).
- Do NOT delete the Phase 3 section body — banner-prefix it so forensic audit can still see the original scope.
- Do NOT touch the "Core Value" quote or the "Granularity Notes" section — both still apply.
  </action>
  <verify>
    <automated>grep -q "CANCELLED 2026-05-26 per Phase 2 D-34" .planning/ROADMAP.md &amp;&amp; grep -q "Bracket Scope Decision (revised 2026-05-26)" .planning/ROADMAP.md &amp;&amp; grep -q "02-09-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -q "02-10-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -q "02-11-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -q "02-12-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -q "12 plans across 9 waves" .planning/ROADMAP.md &amp;&amp; ! grep -q "Real money, OAuth, public signup, external sports API," .planning/ROADMAP.md &amp;&amp; grep -q "Phase 2 scope expansion + Phase 3 cancellation: 2026-05-26" .planning/ROADMAP.md &amp;&amp; echo "ROADMAP.md edits verified"</automated>
  </verify>
  <done>ROADMAP.md announces Phase 3 cancellation; Phase 2 plan list includes 02-09..02-12; Out-of-Scope has bracket-prediction + auto-grade entries; the terse OOS list no longer mentions external sports API. Progress Table reflects new counts.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md — add scope-expansion REQ IDs + traceability + mark BRK-01..04/VIS-03/SCR-03 as OOS</name>
  <read_first>
    - .planning/REQUIREMENTS.md (the entire file; in particular the traceability table around line 200+ and the section headers like "League Mode", "Props/Wildcards", "Visibility", "Scoring Engine" — to learn the existing ID format, checkbox state convention, and traceability table column shape)
    - .planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-CONTEXT.md (D-34..D-47 — defines what each new REQ ID must say)
  </read_first>
  <action>
Make the following exact textual edits to `.planning/REQUIREMENTS.md`:

**Edit 1: Add a new section "Scope Expansion (added 2026-05-26)" at the appropriate place**

INSERT a new section header `### Scope Expansion (added 2026-05-26)` AFTER the existing "Ship-Gate Quality" section (which ends around the QA-01..04 bullets) and BEFORE the "v2 Requirements" / "Out of Scope" section markers (whichever comes first).

The new section body:
```
### Scope Expansion (added 2026-05-26)

Scope-expansion requirements derived from `.planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-CONTEXT.md` `<scope_expansion_addendum>` (D-34..D-47, 2026-05-26). These ship in Phase 2 Plans 02-09..02-12 by the original June 11 hard deadline.

#### Project documentation updates (PROJECT-UPD)

- [ ] **PROJECT-UPD-01**: PROJECT.md "Out of Scope" line for "Live external sports API integration" REMOVED (per D-36)
- [ ] **PROJECT-UPD-02**: PROJECT.md "Out of Scope" gains "Bracket Mode as a prediction game" + "Auto-grade props from public sources" (per D-34 + D-35); Key Decisions table marks D-34 / D-36 / D-38 outcomes; ROADMAP.md Phase 3 banner-marked as CANCELLED
- [ ] **PROJECT-UPD-03**: REQUIREMENTS.md traceability table updates for cut/moved IDs (BRK-01..04, VIS-03, SCR-03 → Out-of-Scope; PRP-01..04 + VIS-04 traceability annotations updated for D-38 privacy)

#### Props strictly private + relocation (PRIVATE)

- [ ] **PRIVATE-01**: `prop_answers_read` RLS policy tightened to `user_id = (select auth.uid())` only — no post-kickoff exists-clause; verified via `bash scripts/verify-rls-no-leak.sh` returning ALL TABLES PASS (per D-38)
- [ ] **PRIVATE-02**: Props page relocates from `/[locale]/props` to `/[locale]/me/props`; old route returns 301 redirect to the new path (per D-37 + nav D-37)
- [ ] **PRIVATE-03**: `/[locale]/me/props` page simplified to remove `isRevealed` branch — user always sees own answers only, pre-lock editable / post-lock read-only receipt (per D-38 + D-39)
- [ ] **PRIVATE-04**: `/[locale]/me` page gains a "Props" card linking to `/[locale]/me/props` with a status pill indicating editable/locked state, plus a body line referencing the June 11 19:00 UTC deadline (per D-37 + D-39). [Revision iteration 2 (2026-05-26): pill copy relaxed from "Editable until June 11 19:00 UTC" to short-form "Editable"/"Locked" so the pill fits at 360px without wrapping in both locales; the long-form deadline lives in the body line. The pill is a status indicator, not a deadline indicator.]

#### Read-only bracket view (BRK-VIEW)

- [ ] **BRK-VIEW-01**: `/[locale]/bracket` server-renders the WC 2026 knockout tree as a column-of-rounds RSC reading `bracket_slots` joined to `fixtures` + `teams` in a single Supabase query (per D-40 + D-47)
- [ ] **BRK-VIEW-02**: Bracket view live-fills via existing admin `saveResult` Server Action: each KO match result triggers `revalidatePath('/he/bracket')` + `revalidatePath('/en/bracket')`; admin re-entry recomputes immediately (per D-40 + D-47)
- [ ] **BRK-VIEW-03**: `bracket_slots.resolved_team_id` writeback added to `saveResult` for non-group fixtures — winner ID computed from `result_home_90min`/`result_away_90min` 90-minute decision; tied KO matches at 90 min leave `resolved_team_id` NULL until Phase 3 ET handling (per D-47)
- [ ] **BRK-VIEW-04**: Champion slot (`slot_code = 'CHAMPION'`) populated by propagating the FINAL match winner to `CHAMPION.resolved_team_id` as part of the same `saveResult` writeback path (per Research Addendum D-40 "Open risks #1")
- [ ] **BRK-VIEW-05**: Bracket view renders correctly at 360px in both `/he/` (RTL) and `/en/` (LTR) using Tailwind v4 logical-property utilities only (no `pl-*`/`pr-*`/`left-*`/`right-*`/`flex-row-reverse`) per Phase 1 FND-03 lint convention

#### Auto-fetch match scores (AUTO)

- [ ] **AUTO-01**: New migration `0014_fixtures_auto_fetched_at.sql` adds `fixtures.auto_fetched_at timestamptz NULL` column with comment documenting the admin-overwrite invariant (per D-45 + D-47 + Research Addendum)
- [ ] **AUTO-02**: New `/api/score-fetch` POST route handler with Bearer-token auth (env var `SCORE_FETCH_SECRET`) — rejects 401 without valid Bearer; fetches football-data.org v4 `/competitions/{code}/matches`; resolves fixtures by `(kickoff_at ±5min, home_team.code, away_team.code)` tuple; performs SELECT-then-UPDATE with admin-lock check in JavaScript (no `.or()` filter ambiguity); sweeps predictions via existing `sweepAndUpsert` helper (per D-45 + D-46)
- [ ] **AUTO-03**: New migration `0012_pg_cron_score_fetch.sql` enables `pg_cron` + `pg_net` Postgres extensions and schedules a job that POSTs to `/api/score-fetch` every 15 minutes during the tournament window (per D-45)
- [ ] **AUTO-04**: Admin `saveResult` Server Action sets `auto_fetched_at = NULL` on every UPDATE so a manual admin entry blocks future cron overwrites (per D-45 + D-47 admin-overwrite invariant)
- [ ] **AUTO-05**: Wave-0 unit test asserts all 104 fixtures from `data/wc2026/fixtures.csv` map cleanly against a synthesized football-data.org response shape — catches vendor TLA mismatches before they bite live (per D-46 + Research Addendum "identifier mapping problem")
- [ ] **AUTO-06**: Tournament-window gate inside `/api/score-fetch` short-circuits with `{ok: true, skipped: 'outside-tournament-window'}` when `now()` is outside `tournament.starts_at - 1h` .. `tournament.ends_at + 1d` (per Research Addendum D-44 "Cron schedule choice")
- [ ] **AUTO-07**: STRIDE threat model documented in Plan 02-12: rejection of non-`[\p{L}\d \-.,]` chars in team-name upsert path (stored-XSS defense); Bearer-secret check (auth-bypass defense); SELECT-then-UPDATE admin-lock check (admin-overwrite race defense)
```

**Edit 2: Mark cancelled Phase 3 requirements as OOS**

In the existing "Bracket Mode (Knockout Predictions)" / "Visibility" / "Scoring Engine" sections, ADD a `❌ CANCELLED 2026-05-26 per Phase 2 D-34` annotation to the bullet text of BRK-01, BRK-02, BRK-03, BRK-04, VIS-03, and SCR-03. Do NOT delete them — preserve the bullet text so forensic searches still find them; just append the cancellation marker.

Example transformation:
- Before: `- [ ] **BRK-01**: User can fill out bracket picks ...`
- After: `- [ ] ~~**BRK-01**: User can fill out bracket picks ...~~ — ❌ CANCELLED 2026-05-26 per Phase 2 D-34 (read-only bracket view replaces prediction game; see BRK-VIEW-01..05)`

Apply the same `~~strikethrough~~` + `❌ CANCELLED ...` annotation to all 6 IDs (BRK-01, BRK-02, BRK-03, BRK-04, VIS-03, SCR-03).

**Edit 3: Update the traceability table**

In the traceability table (around line 200+ — find the rows for `BRK-01 | Phase 3 | Pending` etc.):

UPDATE each cancelled row from `Phase 3 | Pending` to `Out-of-Scope | Cancelled 2026-05-26 (D-34)`:
- BRK-01, BRK-02, BRK-03, BRK-04, VIS-03, SCR-03

UPDATE the props rows PRP-01..04 + VIS-04 annotations:
- For each of `PRP-01 | Phase 2 | Pending`, `PRP-02 | Phase 2 | Pending`, `PRP-03 | Phase 2 | Pending`, `PRP-04 | Phase 2 | Pending`, `VIS-04 | Phase 2 | Pending`, KEEP the Phase 2 mapping but append to the Pending column: `Pending — reinterpreted by D-38 (private; see PRIVATE-01..04)`.

ADD new rows for the scope-expansion IDs at the bottom of the traceability table (preserve table format):
```
| PROJECT-UPD-01 | Phase 2 (Plan 02-09) | Pending |
| PROJECT-UPD-02 | Phase 2 (Plan 02-09) | Pending |
| PROJECT-UPD-03 | Phase 2 (Plan 02-09) | Pending |
| PRIVATE-01 | Phase 2 (Plan 02-10) | Pending |
| PRIVATE-02 | Phase 2 (Plan 02-10) | Pending |
| PRIVATE-03 | Phase 2 (Plan 02-10) | Pending |
| PRIVATE-04 | Phase 2 (Plan 02-10) | Pending |
| BRK-VIEW-01 | Phase 2 (Plan 02-11) | Pending |
| BRK-VIEW-02 | Phase 2 (Plan 02-11) | Pending |
| BRK-VIEW-03 | Phase 2 (Plan 02-11) | Pending |
| BRK-VIEW-04 | Phase 2 (Plan 02-11) | Pending |
| BRK-VIEW-05 | Phase 2 (Plan 02-11) | Pending |
| AUTO-01 | Phase 2 (Plan 02-12) | Pending |
| AUTO-02 | Phase 2 (Plan 02-12) | Pending |
| AUTO-03 | Phase 2 (Plan 02-12) | Pending |
| AUTO-04 | Phase 2 (Plan 02-12) | Pending |
| AUTO-05 | Phase 2 (Plan 02-12) | Pending |
| AUTO-06 | Phase 2 (Plan 02-12) | Pending |
| AUTO-07 | Phase 2 (Plan 02-12) | Pending |
```

**Edit 4: Footer**

Update the file's "Last updated" footer (or trailing line) to add: `*Scope expansion (PROJECT-UPD, PRIVATE, BRK-VIEW, AUTO families): 2026-05-26 — derived from Phase 2 CONTEXT addendum D-34..D-47*`.

**Constraints:**
- Preserve all existing markdown structure (heading levels, table pipes, list bullets).
- Do NOT touch FND-* / I18N-* / AUTH-* / DATA-* / LGE-* / ADM-* / SCR-01/02/04/05/06/07 / LB-* / QA-* / VIS-01/02/05/06 — these are unaffected.
- BRK / VIS-03 / SCR-03 strikethrough annotation must preserve the original bullet text so the file's full traceability remains auditable.
- New REQ IDs use a hyphenated prefix convention matching FND-/AUTH- existing style.
  </action>
  <verify>
    <automated>grep -q "PROJECT-UPD-01" .planning/REQUIREMENTS.md &amp;&amp; grep -q "PRIVATE-01" .planning/REQUIREMENTS.md &amp;&amp; grep -q "BRK-VIEW-01" .planning/REQUIREMENTS.md &amp;&amp; grep -q "AUTO-01" .planning/REQUIREMENTS.md &amp;&amp; grep -q "AUTO-07" .planning/REQUIREMENTS.md &amp;&amp; grep -q "BRK-VIEW-05" .planning/REQUIREMENTS.md &amp;&amp; grep -q "PRIVATE-04" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Scope Expansion (added 2026-05-26)" .planning/REQUIREMENTS.md &amp;&amp; grep -q "❌ CANCELLED 2026-05-26" .planning/REQUIREMENTS.md &amp;&amp; grep -q "reinterpreted by D-38" .planning/REQUIREMENTS.md &amp;&amp; grep -c "Phase 2 (Plan 02-12)" .planning/REQUIREMENTS.md | grep -v '^#' | awk '{ if ($1 < 7) { print "FAIL: expected at least 7 AUTO rows in traceability"; exit 1 } else { print "OK" } }' &amp;&amp; echo "REQUIREMENTS.md edits verified"</automated>
  </verify>
  <done>REQUIREMENTS.md has a new "Scope Expansion" section listing 19 new REQ IDs across 4 families; BRK-01..04 / VIS-03 / SCR-03 are strikethrough-marked as cancelled; PRP-01..04 + VIS-04 carry the D-38 reinterpretation annotation; traceability table lists all 19 new REQ IDs as Phase 2 Pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown editor → repo | Doc-only edits; no code execution surface; no runtime authorization implications |
| Git commit → remote (GitHub) | The three docs are public-information artifacts; no secrets, no operational credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | I (Information Disclosure) | PROJECT.md updated text | accept | Doc-only; no PII, no credentials, no operational surface. Public-information artifacts by design. |
| T-09-02 | T (Tampering) | ROADMAP.md & REQUIREMENTS.md (cross-doc consistency) | mitigate | Task verification grep gates (each task ends in a grep-verifiable assertion) catch silent omissions; commit author convention enforced per Phase 1 P05 |
| T-09-03 | R (Repudiation) | Decision-attribution (D-34..D-47 references) | mitigate | Every cut/reversal cites the D-XX decision number from 02-CONTEXT addendum + the 2026-05-26 date stamp — full forensic chain |

**block_on:** none (low severity; doc-only).
</threat_model>

<verification>
After all three tasks complete, run the following composite check from repo root:

```bash
# All three docs reflect scope-expansion outcomes consistently.
grep -q "Bracket Mode as a prediction game" .planning/PROJECT.md \
  && grep -q "CANCELLED 2026-05-26 per Phase 2 D-34" .planning/ROADMAP.md \
  && grep -q "Scope Expansion (added 2026-05-26)" .planning/REQUIREMENTS.md \
  && grep -q "12 plans across 9 waves" .planning/ROADMAP.md \
  && ! grep -q "Live external sports API integration" .planning/PROJECT.md \
  && echo "ALL THREE DOCS CONSISTENT"
```

Also run `npm run lint` to confirm no lint regressions (these are .md edits, but the lint config may include markdown linting if added later).
</verification>

<success_criteria>
- All four must_haves.truths above hold true after this plan ships.
- `grep -q "Bracket Mode as a prediction game" .planning/PROJECT.md` succeeds.
- `grep -q "12 plans across 9 waves" .planning/ROADMAP.md` succeeds.
- `grep -q "Scope Expansion (added 2026-05-26)" .planning/REQUIREMENTS.md` succeeds.
- `grep -q "Live external sports API integration" .planning/PROJECT.md` FAILS (text removed).
- `git diff --stat` shows exactly 3 files changed: PROJECT.md, ROADMAP.md, REQUIREMENTS.md.
</success_criteria>

<output>
After completion, create `.planning/phases/02-june-11-mvp-league-props-scoring-leaderboard-admin-ship-gate/02-09-SUMMARY.md` with:
- 3-file diff summary (lines added/removed per file)
- Confirmation of grep gates above
- Note any markdown anchor-link breakage discovered during editing (preorder all existing internal links if any)
</output>

<!-- ============================================================ -->
<!-- REVISION ITERATION 2 (2026-05-26) — checker feedback fixes    -->
<!-- ============================================================ -->
<!--
B3 (checker BLOCKER): Resolved with **Option B** (relax PRIVATE-04 text).
  - The PRIVATE-04 bullet in Edit 1 of Task 3 was changed from "with a
    'Editable until June 11 19:00 UTC' / 'Locked' status pill" to the
    short-form pill ("Editable" / "Locked") + a body line carrying the
    June 11 19:00 UTC deadline. Plan 02-10 Task 6 (`propsStatusEditable`
    short form) is therefore compliant with PRIVATE-04 as written.
  - Rationale for Option B over Option A: a long-form pill "Editable until
    June 11 19:00 UTC" risks wrapping/truncation at 360px in both locales
    (Hebrew translation is even longer); status pills are short by
    Phase 1 convention (PtsBadge / WaveBadge / similar). Two-line
    deadline-in-body + status-only-in-pill is less risky and matches
    existing UI patterns.
  - Also updated AUTO-02 and AUTO-07 bullet text to reference the
    SELECT-then-UPDATE refactor (B4 fix in Plan 02-12 Task 6) rather than
    the old `.or()` filter approach.
-->
</content>
</invoke>